{
  "name": "@kryptos/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React UI for Kryptós (web + audit): the stage briefing renderer (StageInfo + presentational sub-components) and the locale context.",
  "scripts": {
    "lint": "eslint",
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    "./stage-images": "./src/stage-images.ts",
    "./*": "./src/*.tsx"
  },
  "dependencies": {
    "@kryptos/core": "*",
    "mermaid": "^11.15.0",
    "react": "19.2.4"
  }
}
