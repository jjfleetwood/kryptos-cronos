// Deep, per-module audit content for the Advanced Audit track. Keyed by module id
// (e.g. "iam-03"). Each record gives the REAL specifics an auditor needs — the
// concrete artifact (the actual file/export/list), the concrete test with PASS
// criteria and what an exception looks like, the real source systems, the real
// tools/commands/APIs to pull the evidence, the typical finding, and references.
//
// The generator (gen-audit-track.mjs) weaves these into each module's briefing,
// auditMeta, CTF, quiz, and the runnable MCP server. Modules without a record here
// fall back to a generic (clearly weaker) template — author them here to deepen.
//
// Fields per record:
//   artifacts: string[]   the concrete evidence objects (a user access list, a
//                         config file, an export) — surfaced as auditMeta.artifacts
//   test:      string     the procedure + PASS criteria + what an exception is
//   systems:   string[]   the real systems of record the evidence comes from
//   owners:    string[]   the role/function that owns each source
//   tools:     string[]   real commands / API calls / queries to gather it
//   finding:   string     what a typical, real finding looks like
//   refs:      [t,url][]   module-specific standards/guidance (optional)

export const MODULES = {
  // ── Identity & Access Management (IAM) ──────────────────────────────────────
  "iam-01": {
    artifacts: [
      "The IdP user export — every account with status, creation date, last-login, and licence/role assignments",
      "The HR worker feed — active workers with hire and termination dates (the authoritative 'who should exist')",
      "The access-request approval tickets that authorised each account at creation",
      "Deprovisioning evidence — disable/delete timestamps to compare against termination dates",
    ],
    test: "Reconcile every IdP account to an active worker in the HR system of record. PASS: each enabled account maps to an active worker; every new account had an approved access request before creation; every terminated worker's accounts were disabled within SLA (e.g. ≤24h privileged, ≤7d standard) and de-licensed. Exceptions: orphaned accounts with no matching active worker, terminated workers still enabled, and accounts created with no approval ticket.",
    systems: ["Okta / Microsoft Entra ID (the identity provider)", "Workday / SAP SuccessFactors (HR system of record)", "ServiceNow (access-request + approval workflow)"],
    owners: ["Identity & Access Management — runs provisioning operations", "HR — owns authoritative worker status and dates", "Line managers — approve the access requests"],
    tools: [
      "Okta:   GET /api/v1/users?limit=200  (status, created, lastLogin, then paginate the Link header)",
      "Entra:  Get-MgUser -All -Property displayName,accountEnabled,createdDateTime,signInActivity",
      "HR:     Workday/SuccessFactors 'active workers + termination date' report (CSV/SOAP)",
      "Trail:  ServiceNow table API /api/now/table/sysapproval_approver?sysparm_query=... for the approval record",
    ],
    finding: "Orphaned accounts with no active worker, and terminated employees whose accounts stayed enabled for days or weeks past their termination date — each named with account id, worker id, and the gap in days.",
    refs: [["NIST SP 800-53 AC-2 Account Management", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["NIST SP 800-63 Digital Identity", "https://pages.nist.gov/800-63-3/"]],
  },
  "iam-02": {
    artifacts: [
      "The joiner-mover-leaver (JML) event log from HR — hires, role/department changes, and terminations in the period",
      "Before/after entitlement snapshots for each worker who changed role",
      "The birthright-role mapping (job code → the access automatically granted)",
      "Leaver deprovisioning timestamps vs the HR termination date",
    ],
    test: "For every MOVER (role/department change) in the period, diff the worker's entitlements before and after. PASS: birthright access updated to the NEW role AND prior-role entitlements removed (no accumulation); joiners got only their role's birthright access; leavers were fully deprovisioned within SLA. Exceptions: 'privilege creep' — movers who kept old-role access on top of new; leavers with residual access; joiners over-provisioned beyond their role.",
    systems: ["HR / HRIS (the JML event source)", "IGA platform — SailPoint / Saviynt / Microsoft Entra ID Governance (entitlements)", "The role / birthright catalogue"],
    owners: ["IAM / Identity Governance — owns the JML automation", "HR — owns the lifecycle events", "Role owners — define each role's birthright access"],
    tools: [
      "IGA:   SailPoint identity-cube entitlement history, or Saviynt 'identity entitlement' export, before vs after the move date",
      "Entra: Get-MgUserMemberOf for each mover at two points in time, then diff the group sets",
      "Okta:  group-membership snapshots (/api/v1/users/{id}/groups) diffed across the move",
      "HR:    JML event extract (effective-dated hires / transfers / terminations)",
    ],
    finding: "Movers who accumulated entitlements across two or three past roles (named, with the stale entitlements listed), and a handful of leavers whose access was removed late.",
    refs: [["NIST SP 800-53 AC-2 / AC-6", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["Gartner — Identity Governance & Administration", "https://www.gartner.com/en/information-technology/glossary/identity-governance-and-administration-iga"]],
  },
  "iam-03": {
    artifacts: [
      "A CSV of every active user joined to their enrolled authentication factors (factor type + enrolment date)",
      "The per-application authentication-policy export showing which apps require MFA and which factor types are allowed",
      "The list of sensitive/regulated applications that the MFA requirement must cover",
      "Sign-in / system logs showing factor usage (to catch policy that exists but isn't enforced)",
    ],
    test: "Reconcile the active-user roster against factor enrolment and the per-app auth policies. PASS: every active human account has at least one phishing-resistant factor (FIDO2/WebAuthn or platform passkey); every application handling confidential/regulated data REQUIRES MFA (not 'optional'); no account relies on SMS or voice as its only factor. Exceptions: active accounts with zero factors or SMS-only, and sensitive apps where MFA is optional or disabled.",
    systems: ["Okta (Universal Directory + Authentication Policies)", "Microsoft Entra ID (Conditional Access)", "The HR system of record for 'active worker'"],
    owners: ["IAM — owns the IdP and the authentication policies", "Application owners — set each app's auth-policy requirement", "HR — the authoritative active-worker list"],
    tools: [
      "Okta:  GET /api/v1/users?filter=status eq \"ACTIVE\"  then  GET /api/v1/users/{id}/factors",
      "Okta:  GET /api/v1/policies?type=ACCESS_POLICY  for the per-application MFA rules",
      "Entra: Get-MgUserAuthenticationMethod -UserId <id>  ;  export Conditional Access policies",
      "Entra: AuthenticationMethods registration & usage report (CSV)",
    ],
    finding: "Typically 3–8% of active accounts have no MFA or are SMS-only, and one or two sensitive apps have MFA set to 'optional' — each named with the user or app id.",
    refs: [["NIST SP 800-63B Authenticator Assurance Levels", "https://pages.nist.gov/800-63-3/sp800-63b.html"], ["CISA — Implementing Phishing-Resistant MFA", "https://www.cisa.gov/resources-tools/resources/implementing-phishing-resistant-mfa"]],
  },
  "iam-04": {
    artifacts: [
      "The privileged-principal inventory — Domain/Enterprise Admins, cloud admin roles, root, local admins, and application super-users",
      "The PAM vault inventory — which privileged credentials are vaulted, rotated, and checkout-controlled",
      "Session checkout / recording logs for privileged access",
      "The standing-vs-just-in-time (JIT) grant list, and break-glass account usage records",
    ],
    test: "Enumerate every privileged principal across directory, cloud, and hosts, then reconcile against the PAM vault. PASS: each is vaulted with automatic rotation, requires MFA + approval/checkout, prefers JIT over standing access, and sessions are logged/recorded; break-glass accounts are sealed and alert on use. Exceptions: privileged accounts NOT in the vault ('discovered, not managed'), unjustified standing admin, shared admin credentials, and break-glass used without a corresponding incident.",
    systems: ["CyberArk / Delinea / BeyondTrust (PAM)", "Active Directory (privileged groups)", "Microsoft Entra PIM + AWS/Azure/GCP IAM (cloud admin roles)"],
    owners: ["Privileged Access Management team — owns the vault and policy", "Platform / infrastructure owners — own the admin accounts", "Security operations — monitor privileged sessions"],
    tools: [
      "AD:      Get-ADGroupMember 'Domain Admins','Enterprise Admins','Schema Admins','Administrators' -Recursive",
      "Entra:   Get-MgRoleManagementDirectoryRoleAssignment  +  PIM eligible vs active assignments",
      "AWS:     aws iam list-entities-for-policy --policy-arn arn:aws:iam::aws:policy/AdministratorAccess  + Access Analyzer",
      "CyberArk: 'accounts discovered (DNA/Discovery) vs onboarded' reconciliation export",
    ],
    finding: "Privileged accounts discovered on hosts and in cloud that are not vaulted, plus standing Domain Admin members beyond the approved break-glass set — each named.",
    refs: [["NIST SP 800-53 AC-6(5) Privileged Accounts", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["CISA — Privileged Access Management guidance", "https://www.cisa.gov/"]],
  },
  "iam-05": {
    artifacts: [
      "The IdP session/token policy export — idle timeout, absolute lifetime, refresh-token lifetime + rotation, reauthentication rules",
      "Per-application session configuration — cookie flags (HttpOnly/Secure/SameSite) and idle/absolute timeouts",
      "Conditional-access / sign-in-frequency policies (step-up reauth, device binding)",
      "Evidence that tokens/sessions are revoked on logout and on deprovisioning",
    ],
    test: "Review session controls across the IdP and the sensitive applications. PASS: idle and absolute session limits are set in proportion to sensitivity (e.g. ≤15-min idle for admin consoles); refresh-token lifetime is bounded and rotated; step-up reauthentication is required for high-risk actions; sessions are revoked on logout and termination; cookies are HttpOnly/Secure/SameSite. Exceptions: unbounded or over-long sessions on sensitive apps, no step-up, refresh tokens never rotated, sessions not revoked on deprovision.",
    systems: ["Okta / Microsoft Entra ID (session + token policies)", "The applications' own session configuration", "API gateway / reverse proxy (token validation)"],
    owners: ["IAM — owns IdP session policy", "Application owners — own app-level session config", "AppSec — reviews cookie/token handling"],
    tools: [
      "Okta:  GET /api/v1/policies?type=ACCESS_POLICY  (session rules) and the global session policy export",
      "Entra: Conditional Access 'Sign-in frequency' + 'Persistent browser session' policy export",
      "App:   inspect Set-Cookie flags + documented idle/absolute timeouts for each sensitive app",
      "Token: confirm refresh-token rotation + revocation-on-logout in the OAuth/OIDC config",
    ],
    finding: "An admin application with a 12-hour idle session and refresh tokens that never rotate, so a stolen token stays valid far beyond the user's presence.",
    refs: [["OWASP Session Management Cheat Sheet", "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html"], ["NIST SP 800-63B — Reauthentication", "https://pages.nist.gov/800-63-3/sp800-63b.html"]],
  },
  "iam-06": {
    artifacts: [
      "The access-certification campaign records — reviewer, items reviewed, approve/revoke decisions, and timestamps",
      "The entitlement catalogue under review (especially high-risk/privileged entitlements)",
      "Revocation evidence for every 'revoke' decision, with the time-to-revoke",
      "Rubber-stamp metrics — approve-all rate and time-per-decision per reviewer",
    ],
    test: "Examine the most recent recertification campaign for in-scope high-risk entitlements. PASS: every high-risk entitlement was reviewed by an accountable owner within the cycle (e.g. quarterly for privileged, annually for standard); 'revoke' decisions were actually executed within SLA; and no reviewer certified their own access. Exceptions: entitlements never included in any campaign, revokes decided but not executed, a reviewer approving 100% of items in minutes (rubber-stamping), or reviewer = grantee.",
    systems: ["SailPoint / Saviynt / Microsoft Entra ID Access Reviews (the IGA campaign engine)", "The IGA entitlement catalogue", "ITSM (to correlate revoke decisions with actual removals)"],
    owners: ["IAM / Identity Governance — runs the campaigns", "Entitlement / business owners — perform the reviews", "Internal audit — independent assurance"],
    tools: [
      "SailPoint: certification campaign export (item-level decisions + decision timestamps)",
      "Entra:    GET /identityGovernance/accessReviews/definitions/{id}/instances/{id}/decisions",
      "Correlate: each 'revoke' decision → the deprovisioning ticket/event that executed it",
      "Analytics: per-reviewer approve-rate and median seconds-per-decision (rubber-stamp signal)",
    ],
    finding: "A privileged group that appears in no certification campaign, and 'revoke' decisions with no corresponding removal 60+ days later — the review happened on paper but changed nothing.",
    refs: [["NIST SP 800-53 AC-2(j) Account Reviews", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["SOX ITGC — periodic access reviews", "https://pcaobus.org/"]],
  },
  "iam-07": {
    artifacts: [
      "The non-human / service-account inventory — each account with a named human owner and documented purpose",
      "The local-administrator inventory per host, and whether each is LAPS-managed (unique per host)",
      "The generic/shared-account list and whether each permits interactive logon",
      "Password age / rotation and vaulting status for every service account",
    ],
    test: "Inventory every service, generic, and local account. PASS: each has a named human owner and documented purpose; service accounts are non-interactive, least-privileged, and vaulted + rotated (or gMSA); local-admin passwords are unique per host (Windows LAPS); no shared interactive generic accounts exist. Exceptions: ownerless service accounts, service accounts in privileged groups, shared interactive logins, identical local-admin passwords reused across hosts, and passwords years old.",
    systems: ["Active Directory (service accounts, gMSA, adminCount)", "Windows LAPS (local-admin password management)", "PAM vault (service-account custody)", "Host/endpoint inventory"],
    owners: ["IAM — owns the account standard", "Server / application owners — own their service accounts", "Platform security — owns LAPS + vaulting"],
    tools: [
      "AD:   Get-ADUser -Filter {ServicePrincipalName -like '*'} -Properties PasswordLastSet,adminCount,MemberOf",
      "AD:   find service accounts where PasswordLastSet is years old, or with adminCount=1 (privileged)",
      "LAPS: report ms-Mcs-AdmPwd / Windows-LAPS attribute coverage across the host estate",
      "Risk: list Kerberoastable SPNs (service accounts with weak/old passwords exposed to offline cracking)",
    ],
    finding: "A service account with a 5-year-old password sitting in Domain Admins, and a single local-admin password reused across 400 workstations (one cracked hash = lateral movement everywhere).",
    refs: [["Microsoft — Windows LAPS", "https://learn.microsoft.com/windows-server/identity/laps/laps-overview"], ["NIST SP 800-53 IA-5 Authenticator Management", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"]],
  },
  "iam-08": {
    artifacts: [
      "The entitlement-to-user assignment export (who holds what, across apps and cloud)",
      "The Segregation-of-Duties (SoD) ruleset — the toxic combinations (e.g. create-vendor + approve-payment)",
      "The SoD violations report — users currently holding conflicting entitlements",
      "Mitigating-control evidence for any conflict that was risk-accepted",
    ],
    test: "Run the SoD ruleset against current assignments and assess least privilege. PASS: no user holds a toxic combination without an approved, real mitigating control; entitlements trend toward least privilege (unused/over-broad access is removed). Exceptions: active SoD conflicts (e.g. a user who can both create and approve payments), over-entitled accounts with access far beyond their role, and conflicts 'accepted' with no genuine compensating control.",
    systems: ["ERP GRC — SAP GRC Access Control / Oracle Risk Management (financial SoD)", "IGA platform (cross-app entitlements)", "Cloud IAM (AWS/Azure/GCP policies)"],
    owners: ["IAM / GRC — owns the ruleset", "Business process owners — own the SoD policy for their process", "Internal audit / Finance — for financially-significant SoD"],
    tools: [
      "SAP GRC: Access Risk Analysis (ARA) report — user-level risk violations against the ruleset",
      "IGA:     export entitlement assignments and join against the toxic-combination ruleset",
      "AWS:     IAM Access Analyzer 'unused access' + Access Advisor last-accessed (least-privilege evidence)",
      "Cloud:   flag policies granting wildcard '*' actions or '*' resources",
    ],
    finding: "A dozen users who can both create and approve vendor payments with no compensating control, and cloud roles granting wildcard '*' actions far beyond what the workload uses.",
    refs: [["NIST SP 800-53 AC-5 (SoD) / AC-6 (least privilege)", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["ISACA — Segregation of Duties", "https://www.isaca.org/"]],
  },
  "iam-09": {
    artifacts: [
      "The certificate inventory — CN/SAN, issuer, key type/size, signature algorithm, expiry, and where each cert is deployed",
      "The CA hierarchy and issuance policy (what may be issued, to whom, with what constraints)",
      "Renewal evidence — which certs auto-renew (ACME) vs are renewed manually",
      "Private-key protection evidence — HSM vs filesystem, and who can read the key",
    ],
    test: "Inventory all TLS, client, and code-signing certificates. PASS: every certificate is inventoried and owned, uses approved algorithms and key sizes (RSA-2048+/ECDSA-P256+, SHA-256+), is not expired or expiring-without-an-owner, auto-renews where possible, and has its private key HSM-protected with least-privilege access; no self-signed certs in production and no unmanaged wildcard sprawl. Exceptions: unknown or expired certs serving production, weak keys (RSA-1024/SHA-1), private keys world-readable on disk, and certs expiring with no renewal owner.",
    systems: ["Venafi / Keyfactor / AWS Certificate Manager / internal PKI (AD CS)", "The issuing CA(s)", "Load balancers, web servers, and service meshes where certs terminate"],
    owners: ["PKI / Cryptography team — owns the CA and policy", "Platform / application owners — own the endpoints where certs are deployed"],
    tools: [
      "Inventory: Venafi/ACM export (CN, issuer, key, expiry, deployment)",
      "Discovery: query Certificate Transparency logs (crt.sh) for all certs issued for the org's domains",
      "Endpoint: nmap --script ssl-cert / openssl s_client -connect host:443 (algorithm + expiry of what's actually served)",
      "AD CS:    the issued-certificates database (templates, key archival, weak-key issuance)",
    ],
    finding: "Three expired certificates still serving production, several RSA-1024/SHA-1 certificates, and a wildcard certificate whose private key sits unprotected in a web root.",
    refs: [["NIST SP 800-57 Key Management", "https://csrc.nist.gov/projects/key-management"], ["CA/Browser Forum Baseline Requirements", "https://cabforum.org/baseline-requirements-documents/"]],
  },
  "iam-10": {
    artifacts: [
      "The access-policy matrix — for each sensitive resource, what identity + device posture + context is required",
      "The device-trust / posture signals that feed access decisions (compliant, managed, healthy)",
      "Segmentation evidence showing no implicit trust is granted by network location alone",
      "Continuous-evaluation / session-revocation logs (access pulled when risk rises mid-session)",
    ],
    test: "Sample sensitive resources and verify access requires a verified identity AND device posture AND context on every request — not network location. PASS: policies enforce least privilege per session, require MFA + a compliant/managed device, and continuously evaluate risk to revoke access; there is no 'on the corporate LAN = trusted'. Exceptions: resources reachable on the LAN with no auth/posture check, a VPN that grants flat subnet access, and policies keyed only to source IP.",
    systems: ["Microsoft Entra Conditional Access / Okta + device trust (Intune / Jamf)", "ZTNA — Zscaler / Cloudflare Access / Netskope", "Network microsegmentation (Illumio / NSX / cloud security groups)"],
    owners: ["IAM — owns identity-based access policy", "Network security — owns segmentation/ZTNA", "Endpoint — owns device posture", "Security architecture — owns the zero-trust target state"],
    tools: [
      "Entra:  export Conditional Access policies + confirm Continuous Access Evaluation (CAE) is enabled",
      "Posture: Intune device-compliance state feeding device-based Conditional Access",
      "ZTNA:   export the per-application access policy (identity + posture + context conditions)",
      "Test:   attempt to reach a sensitive resource from an untrusted segment / non-compliant device and confirm it is denied",
    ],
    finding: "An administrative console reachable on the corporate LAN with no device-posture requirement, and a VPN that drops users onto a flat subnet with broad reachability — classic implicit network trust.",
    refs: [["NIST SP 800-207 Zero Trust Architecture", "https://csrc.nist.gov/pubs/sp/800/207/final"], ["CISA Zero Trust Maturity Model", "https://www.cisa.gov/zero-trust-maturity-model"]],
  },
  "iam-11": {
    artifacts: [
      "The cryptographic inventory / CBOM (Cryptographic Bill of Materials) for certificates and key-exchange — algorithms, key sizes, and where each is used",
      "The list of long-lived certificates/keys and the sensitivity + retention of the data they protect",
      "Crypto-agility evidence — whether cert/key algorithms can be rotated centrally without re-architecting apps",
      "The vendor / PKI post-quantum (PQC) roadmap and the org's migration plan",
    ],
    test: "Assess the certificate and PKI estate for quantum readiness. PASS: a complete CBOM exists; the classical algorithms in use are catalogued; crypto is agile (cert and key types can be rotated centrally); a migration plan to NIST PQC standards (ML-KEM / ML-DSA, FIPS 203/204) and hybrid certificates exists with timelines aligned to CNSA 2.0; and long-lived secrets are prioritised for harvest-now-decrypt-later (HNDL) risk. Exceptions: no crypto inventory, hardcoded/un-agile algorithms, long-lived RSA/ECC protecting data that must outlive a cryptographically-relevant quantum computer, and no vendor PQC commitment.",
    systems: ["PKI with CBOM support — Venafi / Keyfactor", "The TLS + code-signing estate", "KMS / HSM", "Vendor and third-party PKI roadmaps"],
    owners: ["Cryptography / PKI team — owns the CBOM and migration", "Enterprise architecture — owns crypto-agility", "Vendor management — tracks supplier PQC readiness"],
    tools: [
      "CBOM:  generate a cryptography Bill of Materials (e.g. CycloneDX crypto-assets) across the estate",
      "Inventory: Venafi/Keyfactor crypto inventory (algorithm + key size + validity + location)",
      "Scan:  probe endpoints for negotiated key-exchange and any hybrid PQC support",
      "Map:   join data sensitivity + retention period against certificate/key validity to score HNDL exposure",
    ],
    finding: "No CBOM exists; long-lived RSA-2048 certificates protect records with a 25-year retention requirement (squarely in HNDL range); and no key vendor has a published PQC timeline.",
    refs: [["NIST FIPS 203/204/205 (PQC standards)", "https://csrc.nist.gov/projects/post-quantum-cryptography"], ["NSA CNSA 2.0", "https://www.nsa.gov/Cybersecurity/Post-Quantum-Cybersecurity-Resources/"]],
  },

  // ── Vulnerability & Patch Management ────────────────────────────────────────
  "vpm-01": {
    artifacts: [
      "The approved hardening baseline per OS (the CIS Benchmark level or DISA STIG the org committed to)",
      "A configuration-compliance scan of in-scope servers scored against that baseline",
      "The golden-image definition with its build date/version (what new servers are built from)",
      "The exception register for accepted deviations from the baseline",
    ],
    test: "Scan every in-scope server against the approved hardening baseline (CIS Benchmark / DISA STIG for that OS). PASS: each server is built from the current golden image and scores at or above the agreed threshold (e.g. ≥90% CIS Level 1), with documented, time-boxed exceptions for the remainder. Exceptions: servers built off-image or never hardened, baselines scoring below threshold, and 'temporary' exceptions with no expiry.",
    systems: ["CIS-CAT / Tenable Policy Compliance / Qualys PC (configuration-compliance scanner)", "The image pipeline — Packer / golden AMI / VM template", "CMDB (the authoritative in-scope server list)"],
    owners: ["IT Operations / Platform engineering — owns the build", "Security engineering — owns the baseline standard", "Server owners — own any exceptions"],
    tools: [
      "CIS-CAT Pro Assessor run against the CIS Benchmark for each OS (machine-readable score)",
      "Tenable.sc audit policy ('CIS …' / 'DISA STIG …') or Qualys Policy Compliance scan",
      "AWS:  compare each instance's running AMI id against the approved golden-AMI list",
      "Chef InSpec / Ansible compliance profile run against the baseline",
    ],
    finding: "A subset of production servers built from a two-year-old image scoring ~60% against CIS Level 1 — SSH root login enabled, weak ciphers, and no host firewall.",
    refs: [["CIS Benchmarks", "https://www.cisecurity.org/cis-benchmarks"], ["DISA STIGs", "https://public.cyber.mil/stigs/"], ["NIST SP 800-123 Server Security", "https://csrc.nist.gov/pubs/sp/800/123/final"]],
  },
  "vpm-02": {
    artifacts: [
      "The patch-compliance report by host — missing patches with severity and age",
      "The patch SLA policy (e.g. critical ≤7 days, high ≤30 days)",
      "The CISA Known Exploited Vulnerabilities (KEV) list mapped to the estate",
      "The risk-acceptance/exception register for systems that can't be patched on time",
    ],
    test: "Reconcile missing patches against the SLA and the KEV catalogue. PASS: every missing patch is within its severity SLA window; nothing on the CISA KEV list is open past the BOD 22-01 due date; and every overdue item has a formal, time-boxed risk acceptance with a compensating control. Exceptions: critical or KEV-listed vulnerabilities open past SLA, patches missing for months, and 'exceptions' with no owner or expiry date.",
    systems: ["WSUS / SCCM / Intune (Windows) and Satellite / Ansible (Linux) patch state", "Vulnerability scanner (Tenable / Qualys) for missing-patch detection", "CISA KEV feed", "GRC / ticketing for exceptions"],
    owners: ["IT Operations — applies the patches", "Security operations — tracks SLA + KEV", "Risk management — approves exceptions"],
    tools: [
      "SCCM / Intune update-compliance export per device",
      "Tenable: 'missing patch' plugin family + 'vulnerability age' field, filtered to severity",
      "join scan CVEs to the CISA KEV catalogue (cisa.gov/known-exploited-vulnerabilities-catalog)",
      "Linux: dnf/yum updateinfo list security, apt list --upgradable across the fleet",
    ],
    finding: "Several internet-facing hosts missing a KEV-listed critical patch for 90+ days with no risk acceptance on file — exactly the pattern behind most breaches.",
    refs: [["NIST SP 800-40 Patch Management", "https://csrc.nist.gov/pubs/sp/800/40/r4/final"], ["CISA BOD 22-01 / KEV", "https://www.cisa.gov/known-exploited-vulnerabilities-catalog"]],
  },
  "vpm-03": {
    artifacts: [
      "The desired-state configuration baseline held by the CM tool",
      "Drift reports — hosts that deviated from the baseline since the last enforcement run",
      "The change record correlating to any legitimate deviation",
      "Coverage: hosts enrolled in configuration management vs total in the CMDB",
    ],
    test: "Compare running configuration to the enforced baseline. PASS: a configuration-management engine (Ansible/Chef/Puppet/AWS SSM) runs on schedule across ~100% of hosts; drift is detected and auto-remediated or ticketed within SLA; and every legitimate deviation maps to an approved change. Exceptions: hosts not under configuration management at all, drift left unremediated, and unauthorised changes (drift with no corresponding change record).",
    systems: ["Chef / Puppet / Ansible / AWS SSM State Manager (the desired-state engine)", "Drift dashboards / AWS Config", "Change management (to correlate legitimate deviations)"],
    owners: ["Platform / SRE — owns configuration management", "Security engineering — owns the secure baseline", "Change management"],
    tools: [
      "AWS Config rules + SSM State Manager association-compliance report",
      "Chef Automate / Puppet 'corrective vs intentional changes' report",
      "list hosts NOT enrolled in CM (CMDB minus CM inventory) — the blind spots",
      "correlate drift events to ServiceNow change tickets (authorised vs not)",
    ],
    finding: "About 20% of the fleet isn't enrolled in configuration management, so its drift is invisible; among enrolled hosts, several have firewall/security-group drift unremediated for weeks.",
    refs: [["NIST SP 800-128 Configuration Management", "https://csrc.nist.gov/pubs/sp/800/128/final"], ["CIS Control 4 Secure Configuration", "https://www.cisecurity.org/controls"]],
  },
  "vpm-04": {
    artifacts: [
      "The local/privileged administrator membership per server (who can log on as admin)",
      "Bastion / jump-host + PAM checkout logs for every administrative session",
      "MFA-enforcement evidence for administrative logon (RDP/SSH)",
      "The standing-vs-just-in-time (JIT) admin grant list",
    ],
    test: "Enumerate who holds administrative access to in-scope servers and how they reach it. PASS: admin access is least-privilege, brokered through a bastion/PAM with MFA, checkout, and session logging, prefers JIT over standing, and local admin is LAPS-managed; no direct admin logon from user workstations. Exceptions: broad standing admin, shared admin accounts, administrative logon without MFA, and direct RDP/SSH that bypasses the bastion.",
    systems: ["Active Directory / local groups (server admin membership)", "PAM (CyberArk/Delinea) + bastion host", "MFA/IdP for administrative logon"],
    owners: ["Platform / infrastructure — owns server administration", "PAM team — owns the brokered path", "Security operations — monitors sessions"],
    tools: [
      "per host: members of local Administrators (Windows) / sudoers + wheel (Linux)",
      "AD: who holds 'Allow log on through Remote Desktop' / Remote Desktop Users on servers",
      "PAM session logs: which privileged account was checked out, by whom, when",
      "confirm MFA on RDP/SSH (Duo / Entra) and that direct paths to servers are blocked",
    ],
    finding: "A shared local 'admin' account used by 30 engineers with no MFA and direct RDP from workstations, bypassing the bastion entirely — one phished laptop owns every server.",
    refs: [["NIST SP 800-53 AC-6 / AC-17", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["CIS Control 5 Account Management", "https://www.cisecurity.org/controls"]],
  },
  "vpm-05": {
    artifacts: [
      "The authenticated vulnerability-scan results across the asset estate",
      "Scan-coverage report — assets actually scanned vs total in the CMDB, and authenticated vs unauthenticated",
      "Risk-based prioritisation data — CVSS combined with EPSS and the KEV flag and asset exposure",
      "Remediation SLA tracking + aging report, with rescan-verified closures",
    ],
    test: "Assess the vulnerability-management lifecycle end to end. PASS: scans are authenticated and cover ~100% of the CMDB on cadence; findings are prioritised risk-based (CVSS + EPSS + KEV + exposure, not raw CVSS); remediation meets SLA; and closure is verified by rescan. Exceptions: large unscanned or unauthenticated coverage gaps, prioritisation by CVSS alone, SLA breaches, and findings 'closed' without a confirming rescan.",
    systems: ["Tenable / Qualys / Rapid7 (the scanner)", "CMDB (the coverage denominator)", "FIRST EPSS + CISA KEV (prioritisation inputs)", "Ticketing (remediation workflow)"],
    owners: ["Security operations / Vulnerability management — owns the program", "Asset & server owners — remediate", "Risk — accepts residual risk"],
    tools: [
      "Tenable.sc/io: scan-coverage + scan-age report; flag unauthenticated scans (credential failures)",
      "coverage = distinct scanned assets ÷ CMDB assets (the gap is your blind spot)",
      "enrich each CVE with its EPSS score (api.first.org/data/v1/epss) and KEV flag",
      "remediation aging buckets (0-30 / 30-90 / 90+ days) by severity",
    ],
    finding: "Scanner coverage is ~70% of the CMDB and ~40% of scans are unauthenticated (so they miss most host vulnerabilities); a backlog of KEV-listed criticals sits unremediated on the unscanned remainder.",
    refs: [["CISA KEV", "https://www.cisa.gov/known-exploited-vulnerabilities-catalog"], ["FIRST EPSS", "https://www.first.org/epss/"], ["CIS Control 7", "https://www.cisecurity.org/controls"]],
  },
  "vpm-06": {
    artifacts: [
      "The host logging configuration (what's collected: authentication, process-creation, PowerShell script-block, Sysmon, EDR telemetry)",
      "Log-forwarding coverage — hosts shipping to the SIEM vs total in the CMDB",
      "The detection use-case list mapped to MITRE ATT&CK techniques",
      "Log retention period + tamper-protection settings",
    ],
    test: "Verify hosts produce and ship security-relevant logs. PASS: required event sources are enabled (authentication, process-creation with command line, PowerShell script-block, Sysmon, EDR), ~100% of hosts forward to the SIEM, logs are retained per policy and tamper-protected, and key ATT&CK techniques have detections. Exceptions: hosts not forwarding (logging blind spots), missing event sources (e.g. process-creation auditing off), retention below policy, and ATT&CK coverage gaps.",
    systems: ["Sysmon / Windows Event Log / auditd (host configuration)", "SIEM — Splunk / Microsoft Sentinel (forwarding + retention)", "EDR telemetry", "MITRE ATT&CK (coverage mapping)"],
    owners: ["Security operations / detection engineering", "Platform — deploys the agents", "Server owners"],
    tools: [
      "SIEM: hosts reporting in the last 24h vs CMDB (forwarding-coverage gap)",
      "audit policy: confirm Event ID 4688 process-creation + command-line capture and 4104 script-block logging are enabled",
      "Sysmon configuration review (process, network, image-load coverage)",
      "ATT&CK Navigator layer of current detections vs the technique list",
    ],
    finding: "About 15% of servers send no logs to the SIEM, and command-line process auditing (4688) is off fleet-wide — so the single most useful host telemetry for investigations is simply missing.",
    refs: [["NIST SP 800-92 Log Management", "https://csrc.nist.gov/pubs/sp/800/92/final"], ["MITRE ATT&CK", "https://attack.mitre.org/"], ["CIS Control 8", "https://www.cisecurity.org/controls"]],
  },
  "vpm-07": {
    artifacts: [
      "The red-team engagement reports with scope and rules of engagement (RoE)",
      "The blue-team detections that fired (or were missed) per attack technique",
      "The purple-team coverage matrix — techniques exercised vs techniques detected",
      "Remediation tracking + retest evidence for findings",
    ],
    test: "Assess the red/blue/purple program. PASS: adversary-emulation exercises run on cadence against realistic ATT&CK techniques with proper RoE; blue-team detection/response is measured (what was detected, MTTD/MTTR); a purple-team matrix shows technique coverage; and findings are remediated and retested. Exceptions: no adversary emulation, tests not mapped to ATT&CK, detections that silently failed and weren't fixed, and findings with no retest.",
    systems: ["Adversary-emulation tooling — MITRE Caldera / Atomic Red Team / Cobalt Strike", "SIEM/EDR (detection evidence)", "ATT&CK Navigator (coverage)", "Findings tracker"],
    owners: ["Offensive security (red)", "SOC / detection engineering (blue)", "Security leadership"],
    tools: [
      "MITRE Caldera or Atomic Red Team to execute techniques safely",
      "for each executed technique, correlate to a SIEM/EDR alert: detected vs missed",
      "build an ATT&CK Navigator coverage layer from the results",
      "track each finding to closure with retest evidence",
    ],
    finding: "Adversary emulation covers only a handful of ATT&CK techniques; several executed techniques (e.g. LSASS credential dumping) produced no alert, and the detection gap was never remediated.",
    refs: [["MITRE ATT&CK", "https://attack.mitre.org/"], ["NIST SP 800-115 Security Testing", "https://csrc.nist.gov/pubs/sp/800/115/final"]],
  },
  "vpm-08": {
    artifacts: [
      "The malware triage/analysis procedure + sandbox detonation reports",
      "IOC extraction + distribution evidence (hashes/domains/IPs pushed to EDR, SIEM, firewall/DNS)",
      "Sample-handling chain-of-custody + containment evidence",
      "Detection-as-code (YARA/Sigma) produced from the analysis and confirmed deployed",
    ],
    test: "Assess malware-analysis capability and sample handling. PASS: suspected malware is detonated in an isolated sandbox (never production), IOCs are extracted and pushed to enforcement points (EDR block, SIEM watchlist, DNS sinkhole), YARA/Sigma rules are authored and deployed, and samples are contained with chain-of-custody. Exceptions: analysis on production systems, IOCs extracted but never operationalised, no detection content produced, and unsafe sample handling.",
    systems: ["Sandbox — Cuckoo / CAPE / Joe Sandbox, or EDR detonation", "EDR / SIEM / firewall / DNS (IOC enforcement)", "Threat-intel platform (MISP)", "YARA/Sigma rule repository"],
    owners: ["SOC / threat analysis / DFIR", "Detection engineering", "Threat intelligence"],
    tools: [
      "detonate in an isolated VLAN sandbox; capture behavioural + network IOCs",
      "push hashes/domains to the EDR block list + DNS sinkhole + SIEM watchlist (confirm live)",
      "author and deploy YARA/Sigma rules; verify the rule is active in the SIEM/EDR",
      "record the campaign + IOCs in MISP for sharing",
    ],
    finding: "Malware is analysed but the IOCs are never fed back into EDR/SIEM block lists, so the same indicators recur weeks later; no YARA/Sigma detections are produced from the work.",
    refs: [["NIST SP 800-83 Malware Incident Prevention", "https://csrc.nist.gov/pubs/sp/800/83/r1/final"], ["MITRE ATT&CK", "https://attack.mitre.org/"]],
  },
  "vpm-09": {
    artifacts: [
      "The inventory of AI-assisted offensive tooling in use, with its authorisation and guardrails",
      "Logs of AI-generated payloads/recon and the human approval gate before execution",
      "Evidence that AI red-team activity stayed within scope/RoE and is fully logged + attributable",
      "Data-handling controls — proof that target/client data isn't sent to ungoverned model endpoints",
    ],
    test: "Assess governance of AI used in offensive security. PASS: AI-assisted red-team tooling is inventoried and authorised; every AI-generated action runs within scope/RoE behind a human-in-the-loop gate; activity is fully logged and attributable; and sensitive target data is not sent to public/ungoverned models. Exceptions: shadow AI offensive tooling, AI actions taken autonomously outside RoE, missing logging/attribution, and target data leaking to public model endpoints.",
    systems: ["The AI/LLM tooling and its API/usage logs", "C2 / emulation integration", "Authorisation / RoE records", "DLP for prompt content"],
    owners: ["Offensive security", "AI governance / responsible-AI", "Security leadership"],
    tools: [
      "inventory AI offensive tools and confirm each engagement's authorisation",
      "review LLM API logs for prompts/outputs tied to the engagement (attribution)",
      "confirm a human-approval gate exists before any AI-driven action executes",
      "DLP/policy check that target or client data is not pasted into public model endpoints",
    ],
    finding: "An engineer uses a public LLM to generate phishing pretexts with real client data pasted into the prompts — outside RoE, unlogged, and a data-leak in its own right.",
    refs: [["NIST AI Risk Management Framework", "https://www.nist.gov/itl/ai-risk-management-framework"], ["MITRE ATLAS", "https://atlas.mitre.org/"]],
  },
  "vpm-10": {
    artifacts: [
      "The pipeline from cloud vulnerability finding (CSPM/scanner) to a ticket with an owner and SLA",
      "Auto-routing rules (finding → owning team by resource tag) + the deduplication logic",
      "SLA + aging report for cloud-infrastructure vulnerability tickets",
      "Closure verification — a rescan confirms the cloud finding is actually gone",
    ],
    test: "Assess the automated cloud-vulnerability ticketing flow (the Security Ticketing Engine). PASS: every cloud finding (CSPM / Inspector / Wiz) auto-creates a ticket routed to the resource owner by tag, with a severity-based SLA, dedup, and closed-loop verification on rescan. Exceptions: findings with no ticket or owner (orphaned risk), tickets with no SLA, duplicate noise drowning real findings, and tickets closed without a confirming rescan.",
    systems: ["CSPM — Wiz / Prisma Cloud / Microsoft Defender for Cloud, or AWS Inspector + Security Hub", "Ticketing — Jira / ServiceNow", "Cloud tagging (ownership)", "The Security Ticketing Engine automation"],
    owners: ["Cloud security — owns the engine", "Account / resource owners — remediate", "Platform — owns the tagging standard"],
    tools: [
      "AWS Security Hub / Inspector findings → EventBridge → ticket-automation Lambda",
      "route by resource tag (owner/team); dedup by finding id to suppress noise",
      "SLA aging dashboard for cloud tickets by severity",
      "auto-close a ticket only after a rescan shows the finding resolved",
    ],
    finding: "About 30% of cloud findings sit on resources with no owner tag, so they generate orphaned tickets nobody actions; criticals on untagged resources age indefinitely.",
    refs: [["AWS Security Hub", "https://docs.aws.amazon.com/securityhub/"], ["Cloud Security Alliance CCM", "https://cloudsecurityalliance.org/research/cloud-controls-matrix"]],
  },

  // ── Network Security ────────────────────────────────────────────────────────
  "net-01": {
    artifacts: [
      "The network segmentation diagram plus the authoritative VLAN/subnet-to-trust-zone mapping",
      "The firewall/ACL rules that enforce each inter-zone boundary (user→server, app→database, corporate↔OT, CDE↔non-CDE)",
      "East-west traffic-flow logs (NetFlow/IPFIX) showing what actually crosses zone boundaries",
      "The list of flat or over-permissive segments — any-to-any reachability",
    ],
    test: "Validate that the defined trust zones are actually enforced, not just drawn. PASS: each in-scope zone boundary is enforced deny-by-default; observed east-west flows match the documented allow-list; and sensitive zones (PCI CDE, OT, the management plane) are isolated. Exceptions: zones reachable any-to-any, a 'segmented' boundary held open by a permit-ip-any rule, and flows in the logs that no rule should allow (shadow connectivity).",
    systems: ["NGFW — Palo Alto / Fortinet / Cisco (zone enforcement)", "Microsegmentation — Illumio / VMware NSX / cloud security groups", "NetFlow/IPFIX or NDR (observed flows)", "Network/CMDB zone documentation"],
    owners: ["Network engineering — owns the topology", "Network security — owns the zone policy", "Application & data owners — declare which zone their assets belong to"],
    tools: [
      "export the firewall rulebase + interface-to-zone mapping (Panorama / FortiManager)",
      "compare the documented zone matrix against the actual inter-zone permit rules",
      "NetFlow analysis: list cross-zone flows that are NOT in the allow-list",
      "Illumio/NSX policy export + traffic explorer to see real east-west connectivity",
    ],
    finding: "The 'segmented' database VLAN is reachable from the entire user VLAN via a legacy permit-any rule, and NetFlow shows workstations talking straight to the database — the segmentation exists only on the diagram.",
    refs: [["NIST SP 800-207 Zero Trust Architecture", "https://csrc.nist.gov/pubs/sp/800/207/final"], ["PCI DSS — Network Segmentation Guidance", "https://www.pcisecuritystandards.org/"], ["CIS Control 12 Network Infrastructure", "https://www.cisecurity.org/controls"]],
  },
  "net-02": {
    artifacts: [
      "The full firewall rule base export — source, destination, service, action, hit-count, age, owner, last-modified",
      "The change tickets that justify each rule (every rule → an approved change)",
      "The rule-recertification records (periodic review that the business need still exists)",
      "Shadowed / expired / overly-permissive rule analysis (any-any, zero-hit, redundant)",
    ],
    test: "Review the firewall rule base for governance. PASS: every rule has a documented business justification, an owner, and an approval ticket; rules are recertified on cadence; there are no any-any or overly-broad permits; unused (zero-hit) and shadowed/redundant rules are removed; and logging is on for denies and sensitive permits. Exceptions: rules with no owner or justification, permit any-any, zero-hit rules left in place for years, and rules with no approval trail.",
    systems: ["Firewall management — Panorama / FortiManager / Cisco FMC", "Firewall analyzer — Tufin / AlgoSec / FireMon", "Change management (rule ↔ ticket)"],
    owners: ["Network security / firewall team — owns the rulebase", "Application owners — justify their rules", "Change management"],
    tools: [
      "export the rulebase with hit-counts + last-hit (e.g. PAN-OS `show rule-hit-count`)",
      "Tufin/AlgoSec rule-cleanup + risk report (any-any, shadowed, unused, redundant)",
      "join each rule to its change-ticket id (the recertification/approval evidence)",
      "flag any permit to a sensitive zone that has logging disabled",
    ],
    finding: "Hundreds of zero-hit rules accreted over a decade, dozens of permit any-any rules with no owner, and rules whose 'temporary, 2019' justification never expired.",
    refs: [["NIST SP 800-41 Firewall Policy", "https://csrc.nist.gov/pubs/sp/800/41/r1/final"], ["PCI DSS Requirement 1", "https://www.pcisecuritystandards.org/"], ["CIS Control 13 Network Monitoring & Defense", "https://www.cisecurity.org/controls"]],
  },
  "net-03": {
    artifacts: [
      "The remote-access inventory — every VPN gateway and ZTNA application, and who is entitled to each",
      "MFA + device-posture enforcement evidence for remote sessions",
      "The access scope each method grants (full-tunnel / flat subnet vs ZTNA per-application)",
      "Session logs + idle/absolute timeout config + the split-tunnel policy",
    ],
    test: "Assess every remote-access path into the network. PASS: all require phishing-resistant MFA and a posture-checked device; access is least-privilege (ZTNA per-application, or tightly-scoped VPN, not flat-subnet); split/full-tunnel is deliberate; sessions time out and are logged; legacy/unused gateways are decommissioned. Exceptions: a VPN without MFA, a VPN that drops users onto a flat internal subnet, no device-posture check, and forgotten internet-facing remote-access gateways.",
    systems: ["VPN gateways — Cisco AnyConnect / Palo Alto GlobalProtect / Fortinet", "ZTNA — Zscaler Private Access / Cloudflare Access / Netskope", "IdP/MFA + MDM posture — Entra/Okta + Intune/Jamf"],
    owners: ["Network security — owns VPN/ZTNA", "IAM — owns MFA", "Endpoint — owns device posture"],
    tools: [
      "enumerate remote-access gateways (external scan + config) and the auth method each uses",
      "confirm MFA + Conditional Access / device posture on every VPN and ZTNA portal",
      "review the access scope: ZTNA per-app policy vs the routes a VPN actually pushes",
      "Shodan/Censys external scan for exposed remote-access portals you forgot existed",
    ],
    finding: "A legacy SSL-VPN appliance, still internet-facing and unpatched, accepts username + password only (no MFA) and routes onto a flat /16 internal network — the textbook ransomware entry point.",
    refs: [["NIST SP 800-207 Zero Trust Architecture", "https://csrc.nist.gov/pubs/sp/800/207/final"], ["CISA — Securing Remote Access / VPN", "https://www.cisa.gov/"], ["NIST SP 800-46 Telework Security", "https://csrc.nist.gov/pubs/sp/800/46/r2/final"]],
  },
  "net-04": {
    artifacts: [
      "The list of network log sources sending to the SIEM (firewall, VPN, DNS, proxy, NetFlow, NDR) vs the device inventory",
      "The firewall deny + sensitive-permit logging configuration",
      "DNS query logging plus the detections built on it (DGA/tunneling/sinkhole)",
      "NDR/flow coverage of north-south AND east-west, with the retention setting",
    ],
    test: "Verify network telemetry is collected, complete, and actioned. PASS: all in-scope network devices forward logs to the SIEM; firewalls log denies and sensitive permits; DNS and proxy logs are collected; NetFlow/NDR covers north-south and key east-west; detections exist for beaconing/exfiltration/lateral movement; and retention meets policy. Exceptions: devices not forwarding (visibility gaps), deny logging off, no DNS logging, NDR blind to east-west, and retention below policy.",
    systems: ["SIEM — Splunk / Microsoft Sentinel", "NDR — Darktrace / Vectra / Corelight (Zeek)", "Firewall / VPN / DNS / proxy log sources", "NetFlow/IPFIX collectors"],
    owners: ["Security operations / detection engineering", "Network engineering — emits the logs", "SOC"],
    tools: [
      "SIEM: which network devices reported in the last 24h vs the network CMDB",
      "confirm firewalls 'log at session end' on deny and sensitive-permit rules",
      "DNS: query logging enabled + detections (NXDOMAIN spikes, known-bad domains, tunneling)",
      "map NDR sensor placement: which segments are actually monitored vs dark",
    ],
    finding: "Perimeter firewall deny-logging is off, internal DNS isn't logged at all, and the NDR only sees north-south traffic — so lateral movement across the data-center east-west fabric is completely invisible.",
    refs: [["NIST SP 800-92 Log Management", "https://csrc.nist.gov/pubs/sp/800/92/final"], ["MITRE ATT&CK — Command & Control / Exfiltration", "https://attack.mitre.org/"], ["CIS Control 13", "https://www.cisecurity.org/controls"]],
  },
  "net-05": {
    artifacts: [
      "The current-state network architecture diagram + data-flow diagrams for crown-jewel systems",
      "The DMZ/tiering design (no direct internet→internal, proxied/filtered egress, isolated management plane)",
      "The external attack-surface inventory — every internet-facing service",
      "Resilience + out-of-band management-network isolation evidence",
    ],
    test: "Review the network architecture against secure-design principles. PASS: defense-in-depth tiers (DMZ → app → data) with no direct internet-to-internal paths; egress is proxied/filtered (no arbitrary outbound); the management plane is out-of-band and isolated from production/user traffic; the external attack surface is inventoried and minimized; and single points of failure are addressed. Exceptions: internal services directly internet-exposed, unrestricted outbound egress, management interfaces on the user/production network, and an unknown external attack surface.",
    systems: ["Network design docs / enterprise-architecture repository", "External attack-surface management — Censys / Shodan / Microsoft Defender EASM", "Firewalls, forward proxies, load balancers"],
    owners: ["Network architecture / engineering", "Security architecture", "Cloud platform (for cloud network design)"],
    tools: [
      "external attack-surface scan (Shodan/Censys/Defender EASM) across the org's IP ranges + domains",
      "review the egress policy: is outbound default-deny via a proxy allow-list?",
      "confirm out-of-band management isolation (separate VRF/VLAN, not reachable from users)",
      "trace data flows for crown-jewel systems and compare to the documented design",
    ],
    finding: "An internet-exposed RDP service and a server management interface (iLO/IPMI) reachable from the user VLAN, plus wide-open outbound egress that would let any compromised host beacon out unimpeded.",
    refs: [["NIST SP 800-53 SC-7 Boundary Protection", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["CISA — Secure Network Architecture", "https://www.cisa.gov/"], ["CIS Control 12", "https://www.cisecurity.org/controls"]],
  },
  "net-06": {
    artifacts: [
      "Each network device's running-config plus the approved hardened baseline to diff against",
      "Config-backup evidence — every device backed up on schedule and proven restorable",
      "Config-drift/compliance reports (AAA/TACACS+, SSHv2-only, SNMPv3, NTP, logging, no unused services)",
      "Firmware/OS version + end-of-life status per device",
    ],
    test: "Assess network-device configuration governance. PASS: every device's config is backed up automatically and version-controlled; configs are diffed against a hardened baseline (TACACS+/AAA, SSHv2 only, SNMPv3, NTP, logging, no telnet/http); drift is detected; firmware is supported and patched; and changes go through change control. Exceptions: devices with no config backup, telnet/SNMPv1/HTTP enabled, default or shared credentials, end-of-life firmware with known CVEs, and out-of-band config changes.",
    systems: ["NCCM — SolarWinds NCM / Oxidized / RANCID / Cisco DNA Center", "TACACS+/RADIUS (device AAA)", "Config-backup store / git", "Device inventory (firmware + EOL)"],
    owners: ["Network engineering — owns device config", "Network security — owns the hardening baseline", "Change management"],
    tools: [
      "Oxidized/RANCID/NCM: confirm every device has a recent successful config backup",
      "diff running-config vs the hardened baseline (CIS Cisco/Juniper Benchmark)",
      "grep configs for `telnet`, `snmp-server community public|private`, `no aaa`, `ip http server`",
      "map device OS versions to vendor EOL dates + PSIRT advisories",
    ],
    finding: "A dozen access switches still run telnet with a shared 'cisco' enable password, several core devices are past end-of-life with known CVEs, and three devices have no config backup at all — no recovery if they fail.",
    refs: [["NIST SP 800-128 Configuration Management", "https://csrc.nist.gov/pubs/sp/800/128/final"], ["CIS Cisco / Juniper Benchmarks", "https://www.cisecurity.org/cis-benchmarks"], ["CISA — Network Device Hardening", "https://www.cisa.gov/"]],
  },
  "net-07": {
    artifacts: [
      "The inventory of AI/ML-driven networking systems (AIOps anomaly detection, intent-based networking, auto-remediation) and the actions each can take",
      "The guardrails on automated changes — human-approval gates, blast-radius limits, tested rollback",
      "Model + training-data governance for the AIOps platform",
      "Audit logs of AI-initiated network changes and their outcomes",
    ],
    test: "Govern AI/ML in network operations. PASS: every AIOps/intent-based system is inventoried with its authority bounded (what it may change automatically vs what needs approval); automated remediation has blast-radius limits and tested rollback; AI-initiated changes are logged, attributable, and reviewable; and the model/data feeding decisions is governed (no poisoning, no sensitive telemetry leaking to an ungoverned vendor model). Exceptions: AIOps with unbounded auto-remediation authority, AI changes with no logging or rollback, anomaly models trained on unvetted data, and config/telemetry streamed to ungoverned external AI.",
    systems: ["AIOps / AI networking — Cisco AI Network Analytics (DNA) / Juniper Mist AI / Arista CloudVision", "Intent-based networking controllers", "The change/automation pipeline", "Model + data governance tooling"],
    owners: ["Network engineering / NetOps — owns the AIOps platform", "Network security — owns the guardrails", "AI governance — owns model/data risk"],
    tools: [
      "inventory AIOps/IBN systems and the scope of changes each can auto-apply",
      "review auto-remediation policy: approval gate, maximum blast radius, rollback test evidence",
      "pull the audit log of AI-initiated changes (what changed, why, outcome, who reviewed)",
      "confirm telemetry/config sent to vendor AI is governed (DLP + contractual data handling)",
    ],
    finding: "An AIOps platform can auto-apply ACL and routing changes fleet-wide with no human gate and no tested rollback — a single false-positive anomaly could black-hole production — while device telemetry streams to a vendor cloud with no data-handling review.",
    refs: [["NIST AI Risk Management Framework", "https://www.nist.gov/itl/ai-risk-management-framework"], ["NIST SP 800-53 CM-3 / SI-4", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"]],
  },

  // ── Data Protection & Privacy ───────────────────────────────────────────────
  "dpp-01": {
    artifacts: [
      "The data-classification policy + scheme (e.g. Public / Internal / Confidential / Restricted)",
      "The classification coverage report — share of repositories/stores/documents labelled vs unlabelled",
      "Sensitive-data discovery scan results — where PII/PHI/PCI actually lives, including unmanaged shares and dev/test",
      "The handling-rule mapping per class (encryption, access, external sharing, retention)",
    ],
    test: "Verify data is classified and handled per its sensitivity. PASS: a classification scheme exists and is applied (labels/tags) across structured and unstructured stores; a discovery scan finds no high-sensitivity data in unclassified or unmanaged locations; and handling rules (encryption, access, external sharing, retention) are enforced per class. Exceptions: large volumes of unlabelled data, PII/PHI/PCI discovered in unmanaged file shares or dev/test, and sensitive data shared externally against its class rule.",
    systems: ["Microsoft Purview Information Protection / Google DLP / BigID / Varonis (classification)", "Data-discovery scanners", "File shares, SharePoint/OneDrive, databases, data lakes"],
    owners: ["Data Protection Officer / Privacy — owns the scheme", "Data owners / stewards — classify their data", "Security — enforces the handling rules"],
    tools: [
      "Microsoft Purview: sensitivity-label coverage report + auto-labelling scan results",
      "BigID / Varonis sensitive-data discovery across file shares + databases",
      "scan dev/test and unmanaged shares for PII/PHI/PCI patterns (SSNs, PANs, MRNs)",
      "map each class to its handling rules and confirm enforcement (e.g. external-share block on Restricted)",
    ],
    finding: "About 60% of SharePoint sites and most file shares are unlabelled; discovery finds unencrypted spreadsheets of customer SSNs in an open 'Public' share and PHI copied into a developer test database.",
    refs: [["NIST SP 800-60 Information Categorisation", "https://csrc.nist.gov/pubs/sp/800/60/v1/r1/final"], ["GDPR Art. 5 / 32", "https://gdpr-info.eu/"], ["ISO/IEC 27001 A.5.12", "https://www.iso.org/standard/27001"]],
  },
  "dpp-02": {
    artifacts: [
      "The encryption inventory — which stores/links use what (algorithm, key source, TLS version)",
      "TLS configuration of internal and external endpoints (versions, cipher suites, certificate validity)",
      "At-rest encryption status of databases, volumes, object stores, and backups",
      "Key-management evidence — KMS/HSM, customer-managed vs provider-default keys, rotation",
    ],
    test: "Verify sensitive data is encrypted in transit, at rest, and (where required) in use, with managed keys. PASS: data-in-transit uses TLS 1.2+ with strong ciphers (no TLS 1.0/1.1, no weak suites); data-at-rest is encrypted on every store holding sensitive data (DB TDE, volume/object encryption, encrypted backups) with KMS/HSM-managed, rotated keys; and field-level / confidential-compute encryption is used where the data class demands it. Exceptions: cleartext internal links carrying sensitive data, unencrypted databases/volumes/backups, weak TLS, and keys that are provider-default or never rotated.",
    systems: ["KMS/HSM — AWS KMS / Azure Key Vault / HashiCorp Vault", "Databases (TDE), storage (volume/object encryption), load balancers (TLS termination)", "TLS scanners — SSL Labs / testssl.sh / nmap"],
    owners: ["Cryptography/PKI + platform security — own encryption + keys", "Application & data owners — own their stores", "Cloud platform"],
    tools: [
      "testssl.sh / nmap --script ssl-enum-ciphers across internal AND external endpoints",
      "cloud: list unencrypted volumes/buckets/DBs (aws ec2 describe-volumes 'Encrypted==false', S3 default-encryption, RDS StorageEncrypted)",
      "confirm DB TDE / column-level encryption on tables holding sensitive data",
      "KMS: rotation enabled + customer-managed vs provider-default keys for sensitive data",
    ],
    finding: "Internal microservice traffic carrying PII runs over plaintext HTTP, two production databases have TDE off, several customer-data S3 buckets use no encryption, and the keys in use have rotation disabled.",
    refs: [["NIST SP 800-57 Key Management", "https://csrc.nist.gov/projects/key-management"], ["NIST SP 800-52 TLS Guidelines", "https://csrc.nist.gov/pubs/sp/800/52/r2/final"], ["PCI DSS Req. 3 / 4", "https://www.pcisecuritystandards.org/"]],
  },
  "dpp-03": {
    artifacts: [
      "The data-retention schedule by data type (legal/regulatory minimums and maximums)",
      "Evidence that retention/expiry is enforced — storage lifecycle/TTL rules, automated purge jobs",
      "Disposal/destruction records — certificates of destruction, crypto-erase logs (NIST 800-88)",
      "An over-retention scan — data still held past its retention period",
    ],
    test: "Verify data is kept only as long as needed and disposed of securely. PASS: a retention schedule exists per data type and is enforced automatically (storage lifecycle/TTL, DB purge jobs); data past retention is deleted; media/data disposal is secure (crypto-erase / NIST 800-88) with records; and legal holds override deletion correctly. Exceptions: data retained indefinitely with no schedule, retention rules defined but not enforced (data still present years past expiry), insecure disposal, and PII kept beyond its lawful basis.",
    systems: ["Storage lifecycle — S3 Lifecycle / Azure Blob lifecycle; DB purge jobs", "Records-management tooling — Microsoft Purview Data Lifecycle Management", "Media-sanitisation / asset-disposal process"],
    owners: ["Records management / Legal — own the schedule", "Data owners — enforce on their stores", "Privacy — owns PII minimisation"],
    tools: [
      "confirm S3 Lifecycle / Blob lifecycle rules + DB retention jobs exist and actually run",
      "scan for objects/records older than their retention period (last-modified vs schedule)",
      "Purview Data Lifecycle Management retention-label coverage report",
      "disposal: certificates of destruction + crypto-erase logs per NIST 800-88",
    ],
    finding: "PII from closed customer accounts is retained indefinitely — a schedule exists on paper but no lifecycle rule enforces it, so eight years of expired data sits in production, expanding breach and GDPR exposure.",
    refs: [["NIST SP 800-88 Media Sanitisation", "https://csrc.nist.gov/pubs/sp/800/88/r1/final"], ["GDPR Art. 5(1)(e) Storage Limitation", "https://gdpr-info.eu/art-5-gdpr/"]],
  },
  "dpp-04": {
    artifacts: [
      "The backup inventory + schedule per system (what's backed up, frequency, RPO)",
      "Backup success/failure logs across the period",
      "Restore-test evidence — actual test restores, RTO achieved, integrity verified",
      "Backup immutability/air-gap + encryption + access-control configuration (ransomware resilience)",
    ],
    test: "Verify backups exist, work, and would survive an attack. PASS: every in-scope system is backed up to its RPO; backups succeed (failures alerted and fixed); restores are tested on a schedule and meet RTO with verified integrity; and backups are immutable/air-gapped, encrypted, and access-restricted so a compromised production admin can't delete them. Exceptions: systems with no backup, silent backup failures, backups never test-restored, and backups reachable/deletable with the same credentials that run production (no ransomware resilience).",
    systems: ["Backup platform — Veeam / Commvault / Rubrik / AWS Backup", "Immutable storage — S3 Object Lock / immutable vault", "The systems being backed up + the CMDB"],
    owners: ["Backup / Infrastructure operations — own backups", "Business continuity — owns RPO/RTO", "Security — owns immutability"],
    tools: [
      "backup platform: success/failure report per system vs the CMDB (coverage + gaps)",
      "pull restore-test records: date, system, RTO achieved, integrity check result",
      "confirm object-lock/immutability + separate credentials / MFA-delete on the backup store",
      "verify backups are encrypted and NOT reachable from the production domain",
    ],
    finding: "A third of production systems show silent backup failures for months, no restore has been tested this year, and backups sit on a share writable by the production domain-admin account — so one ransomware actor encrypts production and the backups together.",
    refs: [["NIST SP 800-34 Contingency Planning", "https://csrc.nist.gov/pubs/sp/800/34/r1/final"], ["CISA #StopRansomware (3-2-1 + immutable)", "https://www.cisa.gov/stopransomware"]],
  },
  "dpp-05": {
    artifacts: [
      "The DLP policy set — sensitive data types detected, on which channels (email, web, endpoint, cloud)",
      "DLP coverage — endpoints/users/channels enrolled vs total",
      "DLP incident logs — detections, blocks, overrides — and their disposition",
      "The egress channels NOT covered by DLP (personal cloud, USB, unmanaged SaaS)",
    ],
    test: "Verify DLP detects and controls sensitive-data egress across channels. PASS: DLP covers the key sensitive types (PII/PHI/PCI/IP) across email, web/upload, endpoint (USB/print), and cloud (CASB); coverage is near-complete; incidents are triaged and trending down; high-risk channels are blocked or restricted; and policy override requires justification. Exceptions: major channels with no DLP (personal webmail, USB, unmanaged SaaS), large coverage gaps, incidents logged but never triaged, and trivial one-click override of blocks.",
    systems: ["DLP — Microsoft Purview DLP / Forcepoint / Symantec; CASB for cloud", "Email security gateway, web proxy / SWG, endpoint DLP agent", "SIEM (incident triage)"],
    owners: ["Data protection / security operations — own DLP", "Privacy — defines the sensitive types", "HR / Legal — handle insider cases"],
    tools: [
      "DLP console: policy inventory per channel + enrolled-endpoint coverage vs CMDB",
      "incident export: detections / blocks / overrides + disposition + trend over time",
      "test the exfil paths yourself — USB copy, personal webmail upload, unmanaged cloud sync — and see what's caught",
      "CASB: sanctioned vs shadow SaaS data movement",
    ],
    finding: "DLP runs on email only; endpoint USB and personal cloud sync are uncovered, so a test file of customer records copies to USB and uploads to personal Google Drive undetected — and the email DLP alerts route to a queue no one reviews.",
    refs: [["NIST SP 800-53 SC-7 / SI-4", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["Microsoft Purview DLP", "https://learn.microsoft.com/purview/dlp-learn-about-dlp"], ["CIS Control 3 Data Protection", "https://www.cisecurity.org/controls"]],
  },
  "dpp-06": {
    artifacts: [
      "The data-security alert inventory (DLP, classification, access-anomaly, exfil) and where each routes",
      "Alert-triage records — time-to-triage, disposition (true/false positive, escalated), MTTR",
      "The runbooks/playbooks for data-exfiltration and insider alerts",
      "The backlog of un-triaged / aged data alerts",
    ],
    test: "Verify data-security alerts are actually worked, not just generated. PASS: alerts route to a named queue/owner with a triage SLA; true positives are escalated to incident response; the false-positive rate is managed by tuning (not ignored); playbooks exist for exfiltration/insider cases; and the aged-alert backlog is small. Exceptions: alerts firing into an unmonitored mailbox, no triage SLA, a large backlog of un-triaged alerts, no tuning (alert fatigue), and no playbook for confirmed data exfiltration.",
    systems: ["SIEM / SOAR — Sentinel / Splunk + Cortex XSOAR", "DLP / CASB / classification alert sources", "Case management / ITSM"],
    owners: ["Security operations / SOC — triage", "Data protection — owns the policies generating alerts", "Insider-risk / HR-Legal — for people cases"],
    tools: [
      "SIEM/SOAR: data-alert volume, time-to-triage, disposition mix, aged-backlog metrics",
      "confirm each alert source routes to a monitored queue with an owner + SLA",
      "review the exfil/insider playbook and a sample of recent cases for adherence",
      "false-positive rate + tuning history (the alert-fatigue signal)",
    ],
    finding: "DLP and access-anomaly alerts route to a shared mailbox no one owns; ~4,000 alerts sit un-triaged, and a confirmed large download by a departing employee was buried in the noise and never escalated.",
    refs: [["NIST SP 800-61 Incident Handling", "https://csrc.nist.gov/pubs/sp/800/61/r2/final"], ["MITRE ATT&CK — Exfiltration", "https://attack.mitre.org/tactics/TA0010/"]],
  },
  "dpp-07": {
    artifacts: [
      "The Record of Processing Activities (RoPA) — what personal data is processed, why, the lawful basis, and recipients",
      "The data-residency / sovereignty mapping — where each data category is stored/processed vs where it must stay",
      "Cross-border transfer mechanisms (SCCs, adequacy, EU-US DPF) for each export",
      "DSAR + consent + DPIA records",
    ],
    test: "Verify processing of personal data is lawful and data stays where it must. PASS: a RoPA exists with a lawful basis per processing activity; data residency matches regulatory/contractual requirements (e.g. EU data stays in the EU); cross-border transfers have a valid mechanism; DSARs and consent are honoured within statutory timelines; and DPIAs exist for high-risk processing. Exceptions: processing with no lawful basis, regulated data stored/processed in a prohibited region, international transfers with no valid mechanism, missed DSAR deadlines, and high-risk processing with no DPIA.",
    systems: ["Privacy management — OneTrust / TrustArc (RoPA, DSAR, consent)", "Cloud region/residency config + data catalogue", "Contracts / DPAs with processors"],
    owners: ["Data Protection Officer / Privacy — owns compliance", "Legal — owns the transfer mechanisms", "Cloud platform / data owners — control residency"],
    tools: [
      "OneTrust: RoPA completeness + lawful-basis per activity + DSAR-SLA report",
      "cloud: confirm resource regions for PII workloads (e.g. data resident in eu-* only)",
      "map each cross-border data flow to its SCC / adequacy / DPF mechanism",
      "DPIA register for high-risk processing (profiling, large-scale special-category data)",
    ],
    finding: "EU customer data is replicated to a US analytics region with no valid transfer mechanism, the RoPA is two years stale, and ~30% of DSARs missed the 30-day GDPR deadline.",
    refs: [["GDPR", "https://gdpr-info.eu/"], ["ISO/IEC 27701 Privacy Information Management", "https://www.iso.org/standard/71670.html"], ["EU Standard Contractual Clauses", "https://commission.europa.eu/law/law-topic/data-protection/international-dimension-data-protection/standard-contractual-clauses-scc_en"]],
  },
  "dpp-08": {
    artifacts: [
      "The enterprise data inventory/catalogue — datasets with owner, classification, and location",
      "Data-lineage maps for key datasets — source → transformations → consumers/reports",
      "Provenance records for critical/regulatory data (where it originated, how it was derived)",
      "Coverage: catalogued / lineage-mapped datasets vs total, including shadow data stores",
    ],
    test: "Verify the organisation knows what data it has, where it came from, and where it flows. PASS: a data catalogue covers the in-scope estate with owner + classification + location; lineage is mapped for regulatory/critical datasets (so the impact of a change or breach is traceable); provenance is recorded for data used in regulatory reporting or AI training; and shadow/undocumented stores are minimal. Exceptions: large uncatalogued/shadow data, no lineage for regulated datasets (a breach's blast radius or a bad figure can't be traced to source), and missing provenance for reported or AI-training data.",
    systems: ["Data catalogue — Collibra / Alation / Microsoft Purview / DataHub", "Lineage tooling (catalogue-native, or via dbt / Spark)", "The data lakes / warehouses / databases being catalogued"],
    owners: ["Data governance / Chief Data Office — owns the catalogue", "Data stewards — maintain entries", "Data engineering — emits lineage"],
    tools: [
      "catalogue coverage report: catalogued datasets vs discovered stores (the shadow-data gap)",
      "trace lineage for a regulatory dataset end-to-end: source → ETL → report",
      "Purview / Collibra scan to discover uncatalogued sources",
      "provenance check on AI-training and regulatory-reporting datasets",
    ],
    finding: "The catalogue covers the warehouse but misses dozens of shadow databases and extracts; a key regulatory report has no lineage, so when a figure was wrong no one could trace it to source — and the same gap means a breach's blast radius can't be scoped.",
    refs: [["DAMA-DMBOK Data Management", "https://www.dama.org/"], ["NIST SP 800-53 — Data Governance (PM/PT)", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"]],
  },
  "dpp-09": {
    artifacts: [
      "Database/data-store activity-monitoring (DAM) configuration — what access/queries are logged on sensitive stores",
      "Access-anomaly detection on data (unusual volume, off-hours, new principal → sensitive table)",
      "Coverage: sensitive data stores under monitoring vs total",
      "Privileged/DBA access-to-data logging + independent review evidence",
    ],
    test: "Verify access to sensitive data is monitored and anomalies are detected. PASS: database activity monitoring logs access to sensitive stores (who ran what query against which sensitive table, when); privileged/DBA access to data is logged and independently reviewed; anomaly detection flags abnormal access (bulk reads, off-hours, first-time principal → sensitive data); and coverage spans the sensitive estate. Exceptions: sensitive databases with no activity monitoring, DBA access to PII unlogged or self-reviewed, no anomaly detection (a mass export looks normal), and monitoring gaps on cloud data stores.",
    systems: ["DAM — Imperva / IBM Guardium / native (SQL Audit, RDS/Redshift logging)", "SIEM / UEBA for anomaly detection", "The sensitive databases / warehouses / object stores"],
    owners: ["Security operations / data security — own monitoring", "DBAs — emit logs (but don't self-audit)", "Data owners"],
    tools: [
      "Guardium/Imperva or native DB audit: which sensitive stores are monitored vs total",
      "UEBA anomaly rules: bulk reads, off-hours access, first-time access to a sensitive table",
      "review DBA/privileged access-to-data logs — captured AND independently reviewed?",
      "cloud: S3 access logging / CloudTrail data events enabled on sensitive buckets",
    ],
    finding: "Production databases holding PII have no activity monitoring, DBAs query customer data freely with no log or review, and S3 data-event logging is off — so a bulk export of the customer table would leave no trace.",
    refs: [["NIST SP 800-53 AU / AC families", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["PCI DSS Req. 10", "https://www.pcisecuritystandards.org/"]],
  },
  "dpp-10": {
    artifacts: [
      "The inventory of long-lived sensitive data and the confidentiality lifetime each requires",
      "The cryptography protecting that data at rest + in transit (algorithm, key size) — the data-centric CBOM",
      "The harvest-now-decrypt-later (HNDL) exposure assessment",
      "The migration plan to post-quantum cryptography + crypto-agility for data-protection crypto",
    ],
    test: "Assess whether long-lived sensitive data is protected against future quantum decryption. PASS: long-secret data is identified with its required confidentiality lifetime; the crypto protecting it is inventoried; HNDL exposure is scored (data that must stay secret beyond ~2030 and traverses interceptable channels or sits in capturable stores); and a PQC migration plan (NIST FIPS 203/204, hybrid) with crypto-agility exists. Exceptions: long-retention sensitive data protected only by classical RSA/ECC, no HNDL assessment, data-protection crypto that can't be swapped without re-architecture, and no migration timeline.",
    systems: ["Data inventory + retention schedule", "Crypto inventory / CBOM (Venafi/Keyfactor) for data-at-rest + in-transit", "KMS/HSM", "PQC migration tooling"],
    owners: ["Cryptography/PKI + Privacy — own data-protection crypto", "Enterprise architecture — owns crypto-agility", "Data owners — know the retention/secrecy lifetime"],
    tools: [
      "join the retention schedule to data sensitivity → list data needing secrecy beyond ~2030",
      "build a CBOM for data-at-rest (DB/volume/object encryption) + in-transit algorithms",
      "score HNDL: long-secret data on interceptable links or in capturable stores",
      "plan: map current algorithms to NIST PQC (ML-KEM / ML-DSA) + hybrid, with crypto-agility",
    ],
    finding: "Decades-retention health and financial records are protected only by classical RSA/ECC and traverse links an adversary could record today; there is no HNDL assessment and no PQC migration plan, so this data is already at risk.",
    refs: [["NIST FIPS 203/204/205 (PQC)", "https://csrc.nist.gov/projects/post-quantum-cryptography"], ["NIST SP 1800-38 PQC Migration", "https://www.nccoe.nist.gov/crypto-agility-considerations-migrating-post-quantum-cryptographic-algorithms"]],
  },

  // ── Cloud Platform & SaaS ───────────────────────────────────────────────────
  "cld-01": {
    artifacts: [
      "The landing-zone definition (AWS Control Tower / Azure Landing Zone) + the org-level guardrails (SCPs / Azure Policy)",
      "The list of enforced preventive + detective guardrails (deny public object storage, require encryption + logging, region restriction, block root access keys)",
      "The account/subscription baseline applied to every account (logging, IAM, network)",
      "The drift/non-conformance report against the landing-zone standard",
    ],
    test: "Verify every cloud account/subscription is governed by the landing zone's guardrails. PASS: all accounts are vended through the landing zone with org-level preventive guardrails (SCPs / Azure Policy) that local admins can't disable — deny public object storage, require encryption + logging, restrict regions, block root keys; detective controls flag drift. Exceptions: accounts created outside the landing zone (no guardrails), guardrails left in audit-only mode, and accounts where the baseline was detached.",
    systems: ["AWS Control Tower + Organizations SCPs / Azure Landing Zones + Policy / GCP Org Policy", "CSPM — Wiz / Prisma / Defender for Cloud", "The account/subscription inventory"],
    owners: ["Cloud platform / Cloud CoE — owns the landing zone", "Cloud security — owns the guardrails", "Account owners"],
    tools: [
      "aws organizations list-accounts vs Control Tower enrolled accounts (the un-governed gap)",
      "list SCPs + Azure Policy assignments at the org/management-group root; check enforce vs audit mode",
      "CSPM: accounts/subscriptions non-conformant to the baseline",
      "look for accounts in an exempt OU or with guardrail SCPs detached",
    ],
    finding: "A dozen AWS accounts were created directly (not via Control Tower), so they carry no SCP guardrails — public S3 and root access keys are possible there, and CSPM shows several buckets already public.",
    refs: [["AWS Control Tower / Well-Architected", "https://docs.aws.amazon.com/controltower/"], ["CIS AWS Foundations Benchmark", "https://www.cisecurity.org/benchmark/amazon_web_services"], ["CSA Cloud Controls Matrix", "https://cloudsecurityalliance.org/research/cloud-controls-matrix"]],
  },
  "cld-02": {
    artifacts: [
      "The cloud IAM principal inventory (users, roles, service principals) + their effective permissions",
      "The list of admin/privileged roles (Administrator, Owner, iam:*, *:*) and who/what holds them",
      "Unused-access + last-used data (IAM Access Analyzer / Access Advisor / Entra access reviews)",
      "Root / break-glass config (MFA, sealed, monitored) and federation/SSO vs local IAM users + access keys",
    ],
    test: "Assess cloud identity and privilege. PASS: human access is federated via SSO (few/no long-lived IAM users or access keys); privileged roles are least-privilege and just-in-time (e.g. Entra PIM), not standing; root/break-glass is MFA-protected, sealed, and alerts on use; access keys are rotated and unused permissions removed. Exceptions: long-lived IAM users with access keys, standing Administrator/Owner, wildcard `*:*` policies, root with no MFA or in routine use, and access keys unused or un-rotated for ages.",
    systems: ["AWS IAM + IAM Identity Center / Azure Entra ID + PIM / GCP IAM", "Access Analyzer / Access Advisor / Entra access reviews", "CSPM / CIEM — Wiz / Sonrai"],
    owners: ["Cloud security / CIEM — owns cloud identity", "Cloud platform", "IAM"],
    tools: [
      "aws iam list-users + list-access-keys (key age, last-used); flag long-lived keys",
      "find policies granting Action:'*' on Resource:'*' (wildcard admin)",
      "IAM Access Analyzer 'unused access' findings + Access Advisor last-accessed services",
      "Entra PIM: standing vs eligible role assignments; confirm root/global-admin MFA + usage",
    ],
    finding: "Thirty long-lived IAM users with active access keys (several unused for a year), three custom policies granting `*:*`, and the root account has an active access key with no MFA.",
    refs: [["AWS IAM Security Best Practices", "https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html"], ["CIS AWS / Azure Foundations", "https://www.cisecurity.org/cis-benchmarks"], ["NIST SP 800-207", "https://csrc.nist.gov/pubs/sp/800/207/final"]],
  },
  "cld-03": {
    artifacts: [
      "CloudTrail/Activity Log + Config + VPC flow-log + threat-detection (GuardDuty/Defender) enablement status across every account and region",
      "The central log archive (immutable, in a separate security account) + its retention",
      "Detection coverage — GuardDuty/Defender/Security Hub findings flowing to the SIEM",
      "The gaps — accounts/regions with logging off or logs deletable locally",
    ],
    test: "Verify cloud audit + threat logging is on everywhere and tamper-resistant. PASS: management-event logging (CloudTrail/Activity Log) is enabled org-wide across all regions; Config + flow logs + threat detection (GuardDuty/Defender for Cloud) are on; logs ship to a central, immutable archive in a separate security account with retention per policy; and findings reach the SIEM. Exceptions: accounts/regions with no trail, logs writable/deletable by the workload account, threat detection off, and findings that go nowhere.",
    systems: ["AWS CloudTrail / Config / GuardDuty / Security Hub — or Azure Monitor + Defender for Cloud", "Central log-archive account (S3 + Object Lock)", "SIEM"],
    owners: ["Cloud security — owns detection", "Cloud platform — enables logging", "SOC"],
    tools: [
      "aws cloudtrail describe-trails across accounts/regions; confirm an org-trail that is multi-region",
      "GuardDuty/Defender enablement per account; AWS Config recorder status",
      "confirm the log-archive bucket has Object Lock and sits in a separate account with deny-delete",
      "Security Hub → SIEM integration health",
    ],
    finding: "CloudTrail is off in three regions and two accounts entirely; the log bucket lives in the same workload account and is deletable by its admins; GuardDuty is disabled outside the primary region.",
    refs: [["CIS AWS Foundations", "https://www.cisecurity.org/benchmark/amazon_web_services"], ["AWS Security Reference Architecture", "https://docs.aws.amazon.com/prescriptive-guidance/latest/security-reference-architecture/"], ["NIST SP 800-92", "https://csrc.nist.gov/pubs/sp/800/92/final"]],
  },
  "cld-04": {
    artifacts: [
      "The security-group + NACL (or NSG) rule export across the estate",
      "The list of internet-exposed resources (0.0.0.0/0 ingress) and the ports open",
      "Default-VPC / default-security-group usage",
      "VPC flow logs showing actual ingress to exposed resources",
    ],
    test: "Assess cloud network exposure. PASS: no security group allows 0.0.0.0/0 to sensitive ports (SSH 22, RDP 3389, DB 3306/5432, etc.); ingress is least-privilege and justified; default VPCs/SGs aren't used for workloads; private subnets + endpoints keep the data plane off the internet; and flow logs confirm no unexpected ingress. Exceptions: SSH/RDP/DB open to the world, over-broad SG rules, publicly-accessible databases, and default-SG allow-all in use.",
    systems: ["AWS VPC SG/NACL / Azure NSG / GCP firewall rules", "CSPM — Wiz / Prisma", "VPC flow logs"],
    owners: ["Cloud platform / network — own cloud networking", "Cloud security — owns the exposure policy", "App owners"],
    tools: [
      "aws ec2 describe-security-groups → rules with CidrIp 0.0.0.0/0 on 22 / 3389 / 3306 / 5432",
      "CSPM 'publicly exposed' resource inventory",
      "check for RDS / databases with PubliclyAccessible=true",
      "flow logs: accepted ingress from the internet to exposed resources",
    ],
    finding: "Several security groups allow SSH and RDP from 0.0.0.0/0, two RDS databases are publicly accessible, and the default VPC's allow-all security group is attached to production workloads.",
    refs: [["CIS AWS / Azure Foundations", "https://www.cisecurity.org/cis-benchmarks"], ["AWS VPC Security Best Practices", "https://docs.aws.amazon.com/vpc/latest/userguide/vpc-security-best-practices.html"]],
  },
  "cld-05": {
    artifacts: [
      "The SaaS application inventory (M365, Salesforce, Workday, ServiceNow, Slack) + each app's security-config baseline",
      "Each app's posture vs the baseline (SSO + MFA enforced, external sharing, admin roles, audit logging on)",
      "SaaS Security Posture Management (SSPM) scan results for misconfigurations",
      "The third-party OAuth app / integration grants per SaaS tenant",
    ],
    test: "Assess the security configuration of business-critical SaaS. PASS: each major SaaS enforces SSO + MFA, restricts external sharing/guest access per policy, limits admin roles, enables audit logging shipped to the SIEM, and follows a documented baseline (e.g. CIS M365); OAuth app grants are reviewed. Exceptions: SaaS with local logins bypassing SSO, open external sharing, excessive global admins, audit logging off, and risky third-party OAuth grants with broad scopes.",
    systems: ["SSPM — AppOmni / Adaptive Shield / Microsoft Secure Score", "The SaaS admin consoles (M365, Salesforce, Workday, ServiceNow, Slack)", "IdP (SSO)"],
    owners: ["SaaS application owners — own each tenant's config", "Cloud/SaaS security — owns the baseline", "IAM (SSO)"],
    tools: [
      "SSPM scan across connected SaaS for posture drift vs baseline",
      "M365: Secure Score + Conditional Access, external-sharing settings, global-admin count",
      "review OAuth / enterprise-app grants + their scopes per tenant",
      "confirm audit-log export to the SIEM for each SaaS",
    ],
    finding: "Salesforce permits local password logins that bypass SSO/MFA, M365 has 14 global admins and open guest sharing, and a third-party OAuth app holds org-wide 'read all mail' that nobody remembers approving.",
    refs: [["CIS Microsoft 365 Benchmark", "https://www.cisecurity.org/benchmark/microsoft_365"], ["CSA SaaS Governance Best Practices", "https://cloudsecurityalliance.org/"], ["OWASP SaaS Security", "https://owasp.org/"]],
  },
  "cld-06": {
    artifacts: [
      "The detection rules over cloud admin / control-plane activity (CloudTrail / Activity Log)",
      "Alerting on high-risk actions (IAM/policy changes, key/secret access, security-group opened to world, logging disabled, root usage)",
      "The triage record for admin-activity alerts",
      "Coverage — all accounts feeding the detection pipeline",
    ],
    test: "Verify cloud administrative + control-plane activity is monitored for abuse. PASS: high-risk control-plane events are alerted (IAM/policy changes, new access keys, KMS key use, SG opened to 0.0.0.0/0, CloudTrail disabled, root login, mass deletion); alerts cover all accounts and route to a monitored queue; and there's evidence they're triaged. Exceptions: no detections on control-plane events, key risky actions unalerted (e.g. trail-disabled), accounts missing from the pipeline, and alerts no one reviews.",
    systems: ["SIEM (Sentinel/Splunk) + cloud-native (GuardDuty, Defender, Security Hub)", "CloudTrail / Activity Log (event source)", "SOAR / case management"],
    owners: ["Cloud security / detection engineering", "SOC", "Cloud platform"],
    tools: [
      "confirm detections for: PutUserPolicy/AttachRolePolicy, CreateAccessKey, StopLogging, AuthorizeSecurityGroupIngress 0.0.0.0/0, root ConsoleLogin",
      "verify every account feeds the SIEM (coverage)",
      "GuardDuty/Defender alert routing + triage records",
      "test: trigger a benign risky action and confirm it alerts",
    ],
    finding: "Disabling CloudTrail and creating new IAM access keys generate no alert; admin-activity monitoring exists only for the primary account, leaving the rest of the org's control plane unwatched.",
    refs: [["AWS Security Reference Architecture", "https://docs.aws.amazon.com/prescriptive-guidance/latest/security-reference-architecture/"], ["MITRE ATT&CK — Cloud", "https://attack.mitre.org/matrices/enterprise/cloud/"]],
  },
  "cld-07": {
    artifacts: [
      "The cloud workloads' multi-AZ / multi-region design + documented RTO/RPO",
      "Auto-scaling + health-check + self-healing configuration",
      "Backup + cross-region replication evidence for stateful services",
      "DR test / game-day records for cloud failover",
    ],
    test: "Assess resilience of cloud-hosted services. PASS: critical workloads are multi-AZ (and multi-region where RTO/RPO demands), with auto-scaling, health checks, and self-healing; stateful data is backed up and replicated cross-region; DR failover is tested (game days) and meets RTO/RPO; no tier-1 service has a single-AZ/single-region SPOF. Exceptions: tier-1 workloads in a single AZ, no cross-region backup/replication for critical data, DR never tested, and undocumented RTO/RPO.",
    systems: ["The cloud workloads (compute, DB, storage)", "AWS Resilience Hub / Well-Architected (or Azure equivalents)", "Backup + replication services"],
    owners: ["SRE / platform — own resilience", "Application owners — own RTO/RPO", "Business continuity"],
    tools: [
      "map tier-1 workloads to AZ/region spread (single-AZ SPOFs?)",
      "AWS Resilience Hub assessment vs RTO/RPO; check Multi-AZ on RDS, cross-region replication on S3",
      "pull DR game-day records: date, RTO achieved, outcome",
      "review auto-scaling + health-check configuration",
    ],
    finding: "The primary transaction database is single-AZ with no read replica or cross-region backup, and no cloud DR failover has ever been tested — the stated one-hour RTO is aspirational.",
    refs: [["AWS Well-Architected — Reliability Pillar", "https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/welcome.html"], ["ISO 22301", "https://www.iso.org/standard/75106.html"], ["NIST SP 800-34", "https://csrc.nist.gov/pubs/sp/800/34/r1/final"]],
  },
  "cld-08": {
    artifacts: [
      "The inventory of third-party code/components in cloud workloads (container base images, Lambda layers, marketplace AMIs/images, IaC modules)",
      "Image provenance + signing (signed images, approved registries only)",
      "Vulnerability + license scan of images/layers in the registry",
      "The IAM permissions granted to third-party/marketplace tools + cross-account roles",
    ],
    test: "Assess the cloud software + service supply chain. PASS: workloads use only trusted, scanned, signed images from approved registries (no unvetted public/marketplace images); base images + Lambda layers are inventoried and vuln-scanned; IaC modules come from trusted sources; and third-party tools get least-privilege, time-bound cross-account roles (with an ExternalId). Exceptions: untrusted public/marketplace images in production, unsigned images, unscanned base layers with known CVEs, and broad standing cross-account roles to vendors.",
    systems: ["Container registry (ECR/ACR) + image scanning (Trivy / Inspector)", "Sigstore/cosign + admission control (Kyverno / OPA Gatekeeper)", "IaC module registry", "Cross-account role inventory"],
    owners: ["Platform / DevOps — own the images", "Cloud security — owns the supply-chain policy", "Procurement (for vendor roles)"],
    tools: [
      "registry scan: base images + layers with CVEs; flag images from untrusted sources",
      "verify image signing (cosign) + that admission control blocks unsigned images",
      "list IAM roles assumable by external accounts (cross-account trust) + their permissions + ExternalId",
      "inventory marketplace AMIs/images in use vs the approved list",
    ],
    finding: "Production runs several unsigned public Docker Hub images with critical CVEs, and a monitoring vendor holds a standing cross-account role with broad read on all data services and no ExternalId.",
    refs: [["SLSA Supply-chain Framework", "https://slsa.dev/"], ["NIST SP 800-161 C-SCRM", "https://csrc.nist.gov/pubs/sp/800/161/r1/final"]],
  },
  "cld-09": {
    artifacts: [
      "The cloud-provider / managed-service-provider relationship docs — the shared-responsibility matrix per service",
      "The CSP's current assurance evidence (SOC 2, ISO 27001, FedRAMP) and its currency",
      "The MSP/partner access into the tenant (roles, federation, standing vs JIT) + monitoring",
      "Contractual security terms — breach notification, data-handling, residency commitments",
    ],
    test: "Assess reliance on the cloud provider and managed partners. PASS: shared responsibility is documented per service (you know which controls are yours); current CSP attestations (SOC 2 / ISO / FedRAMP) are on file and cover the services used; partner/MSP access is least-privilege, federated, JIT, logged, and reviewed; and contracts carry security, breach-notification, and data-residency terms. Exceptions: unclear shared-responsibility boundaries, stale/missing CSP attestations, MSPs with standing admin into the tenant, and partner access that isn't logged or reviewed.",
    systems: ["CSP trust portals (AWS Artifact / Azure Trust Center)", "Partner/MSP access (cross-tenant roles / Azure Lighthouse / delegated admin)", "TPRM / GRC + contracts"],
    owners: ["Vendor / third-party risk + Cloud security", "Legal (contracts)", "Cloud platform (partner access)"],
    tools: [
      "pull current CSP SOC 2 / ISO / FedRAMP from AWS Artifact / Azure Trust Center; check scope + date",
      "list partner/MSP access (Azure Lighthouse delegations, cross-account roles) + permissions",
      "confirm partner actions are logged in CloudTrail/Activity Log and reviewed",
      "map shared responsibility per service actually used",
    ],
    finding: "An MSP holds standing Owner via Azure Lighthouse across all subscriptions with no JIT or review, and the CSP SOC 2 on file is two years old and doesn't cover two services now in production.",
    refs: [["CSA STAR / Cloud Controls Matrix", "https://cloudsecurityalliance.org/star"], ["Shared Responsibility Model", "https://aws.amazon.com/compliance/shared-responsibility-model/"], ["ISO/IEC 27036", "https://www.iso.org/standard/59648.html"]],
  },
  "cld-10": {
    artifacts: [
      "The cloud-assessment schedule + scope (continuous CSPM, periodic penetration test, config review)",
      "The latest CSPM/benchmark posture report (CIS Foundations score) + the trend over time",
      "Penetration-test reports for cloud-hosted apps + remediation tracking",
      "Coverage — accounts/services assessed vs total",
    ],
    test: "Verify the cloud estate is regularly assessed. PASS: CSPM runs continuously against a recognised benchmark (CIS Foundations) across all accounts with a tracked, improving score; periodic penetration tests cover internet-facing cloud apps with findings remediated and retested; and assessment coverage spans the estate. Exceptions: accounts not in CSPM, no benchmark score/trend, no pen testing of cloud-exposed apps, and findings reported but never remediated.",
    systems: ["CSPM — Wiz / Prisma / Defender for Cloud (continuous benchmark)", "Penetration-test vendor + reports", "Findings tracker"],
    owners: ["Cloud security — owns assessments", "Account owners — remediate", "Risk"],
    tools: [
      "CSPM: CIS Foundations score per account + trend; list accounts not enrolled",
      "pen-test report inventory for cloud apps + remediation/retest status",
      "coverage = assessed accounts ÷ total accounts",
      "compare the current posture score to prior periods (is it improving?)",
    ],
    finding: "CSPM covers 60% of accounts; the CIS Foundations score has been flat for a year (findings reported, not fixed), and the customer-facing cloud app hasn't had a penetration test in two years.",
    refs: [["CIS Cloud Foundations Benchmarks", "https://www.cisecurity.org/cis-benchmarks"], ["NIST SP 800-115", "https://csrc.nist.gov/pubs/sp/800/115/final"]],
  },
  "cld-11": {
    artifacts: [
      "The inventory of cloud data stores (S3/Blob/RDS/Redshift) with classification + encryption + public-access status",
      "Object-storage public-access-block + bucket-policy review",
      "Lifecycle/retention + versioning + encryption configuration per store",
      "Key management — customer-managed key (CMK) vs provider-default, and who can access keys",
    ],
    test: "Assess protection and lifecycle of data in the cloud. PASS: every store holding sensitive data is encrypted with a customer-managed key, has public access blocked, enforces lifecycle/retention + versioning, and logs data access; classification tags drive the controls. Exceptions: public buckets/blobs with sensitive data, default or no encryption, no lifecycle (infinite retention) or no versioning (no ransomware recovery), and keys accessible too broadly.",
    systems: ["S3 / Blob / object storage, RDS / Redshift / managed DBs", "KMS / Key Vault", "CSPM + storage config"],
    owners: ["Data owners + cloud platform", "Cloud security", "Privacy"],
    tools: [
      "aws s3api get-public-access-block + bucket policy across buckets; flag public + sensitive",
      "confirm default encryption + CMK (not the aws/s3 default key) on sensitive stores",
      "lifecycle + versioning config per bucket; RDS encryption + backup retention",
      "KMS key policy: who can use/manage the CMK",
    ],
    finding: "Two S3 buckets holding customer exports are public, several sensitive stores use the default AWS-managed key (no CMK control), and no bucket has versioning — so a ransomware overwrite is unrecoverable.",
    refs: [["CIS AWS Foundations", "https://www.cisecurity.org/benchmark/amazon_web_services"], ["NIST SP 800-57", "https://csrc.nist.gov/projects/key-management"]],
  },
  "cld-12": {
    artifacts: [
      "The data-residency requirement per data category (regulatory + contractual)",
      "The actual region placement of each sensitive workload + its backups, replicas, and logs",
      "Cross-region replication / CDN / support-data flows that could move data out of region",
      "Sovereignty controls — region-restriction SCP / Azure Policy 'allowed locations', sovereign-cloud usage",
    ],
    test: "Verify regulated data stays in its required jurisdiction. PASS: residency requirements are documented per category; sensitive workloads, backups, replicas, and logs reside only in permitted regions; cross-region/CDN/support flows don't move data out of bounds; and region-restriction guardrails prevent deployment elsewhere. Exceptions: regulated data (or its backups/logs) in a prohibited region, replication/CDN egressing data cross-border, and no preventive region guardrail.",
    systems: ["Cloud region/policy config (SCP region-deny / Azure Policy 'allowed locations')", "Backup + replication + CDN config", "Data catalogue (where data lives)"],
    owners: ["Privacy / Legal — own residency requirements", "Cloud platform — enforces region controls", "Data owners"],
    tools: [
      "enumerate resource regions for sensitive workloads (including backups, replicas, log archives)",
      "confirm an SCP region-deny / Azure Policy 'allowed locations' is enforced",
      "check cross-region replication + CDN POP configuration for cross-border data movement",
      "verify support / telemetry data-handling region",
    ],
    finding: "EU customer data is primary in eu-west-1 but its backups replicate to us-east-1 and CloudTrail logs land in a US bucket — and no region-deny SCP prevents deploying EU workloads to US regions.",
    refs: [["GDPR", "https://gdpr-info.eu/"], ["EU Standard Contractual Clauses", "https://commission.europa.eu/law/law-topic/data-protection/international-dimension-data-protection/standard-contractual-clauses-scc_en"]],
  },
  "cld-13": {
    artifacts: [
      "The pipeline turning every cloud security finding (CSPM / GuardDuty / Inspector) into a ticket with an owner + SLA",
      "Auto-routing by resource tag + the deduplication logic",
      "SLA + aging dashboard for cloud security tickets",
      "Closure verification — rescan / auto-resolve on remediation",
    ],
    test: "Assess the engine that operationalises cloud findings into tracked work (the Security Ticketing Engine). PASS: every cloud security finding auto-creates a ticket routed to the resource owner by tag, with a severity-based SLA, dedup, and closed-loop verification on rescan; orphaned (untagged) findings are escalated to a named owner. Exceptions: findings with no ticket/owner, no SLA, duplicate noise drowning real findings, and tickets closed without confirming the finding is gone.",
    systems: ["CSPM / GuardDuty / Inspector / Security Hub (sources)", "Ticketing — Jira / ServiceNow", "EventBridge / automation", "Cloud tagging (ownership)"],
    owners: ["Cloud security — owns the engine", "Resource owners — remediate", "Platform — owns the tagging standard"],
    tools: [
      "Security Hub findings → EventBridge → ticket automation (confirm it runs for ALL finding types, not just Security Hub)",
      "routing rules by tag + dedup by finding id; aging dashboard by severity",
      "untagged-resource findings: are they escalated to an owner or silently dropped?",
      "auto-close a ticket only after a rescan shows it resolved",
    ],
    finding: "Only Security Hub criticals create tickets (GuardDuty + Inspector findings are dropped), ~30% of resources are untagged so their findings have no owner, and tickets auto-close on a timer without confirming remediation.",
    refs: [["AWS Security Hub Automation", "https://docs.aws.amazon.com/securityhub/"], ["Cloud Security Alliance CCM", "https://cloudsecurityalliance.org/research/cloud-controls-matrix"]],
  },

  // ── Cryptographic Key & Secrets Management ──────────────────────────────────
  "cks-01": {
    artifacts: [
      "The key inventory — every cryptographic key with type, algorithm, length, owner, creation date, crypto-period, and state (active/suspended/destroyed)",
      "The key-lifecycle policy (generation, distribution, rotation, revocation, destruction) per key class",
      "Rotation evidence — keys rotated within their crypto-period",
      "Key-state records — retired/destroyed keys with destruction proof",
    ],
    test: "Verify every cryptographic key is managed through its full lifecycle. PASS: a complete key inventory exists; keys are generated with approved algorithms/lengths in a secure module; each key has a defined crypto-period and is rotated within it; revocation works; and retired keys are securely destroyed with records. Exceptions: unknown/untracked keys, keys past their crypto-period (never rotated), keys with no owner, and 'destroyed' keys with no destruction evidence (or still re-enableable).",
    systems: ["KMS — AWS KMS / Azure Key Vault / HashiCorp Vault / Thales CipherTrust", "The key inventory / key-management tooling", "HSM (for high-value keys)"],
    owners: ["Cryptography / Key Management team — owns the lifecycle", "Application owners — own their data keys", "Security"],
    tools: [
      "AWS KMS: aws kms list-keys + get-key-rotation-status (creation date, last-rotated, rotation enabled)",
      "join keys to a documented crypto-period; flag those overdue for rotation",
      "Vault: key/secret metadata + lease/rotation configuration",
      "confirm destroyed keys are scheduled-for-deletion/destroyed, not merely disabled",
    ],
    finding: "Dozens of KMS keys have rotation disabled and predate any crypto-period policy; several 'retired' keys are merely disabled (still re-enableable); and a set of data-encryption keys have no recorded owner.",
    refs: [["NIST SP 800-57 Key Management", "https://csrc.nist.gov/projects/key-management"], ["NIST SP 800-130 Key Management Framework", "https://csrc.nist.gov/pubs/sp/800/130/final"], ["FIPS 140-3", "https://csrc.nist.gov/pubs/fips/140-3/final"]],
  },
  "cks-02": {
    artifacts: [
      "The secrets inventory in the vault (DB passwords, API keys, tokens, service-account creds) + owners + rotation status",
      "Evidence secrets are vaulted, not in code/config/env — a secret-scan of repos, pipelines, and images",
      "Dynamic-secret / short-TTL usage vs static long-lived secrets",
      "The access policy to secrets (who/what can read each) + the secret-access audit log",
    ],
    test: "Verify application/service secrets are centrally vaulted, rotated, and least-privilege. PASS: secrets live in a vault (HashiCorp Vault / cloud Secrets Manager), not in source, config, CI, or env files; they're rotated on a schedule (or dynamic/short-TTL); access is least-privilege per workload identity and fully logged. Exceptions: hardcoded secrets in repos/pipelines/images, long-lived static secrets never rotated, broad read access to all secrets, and secret access that isn't logged.",
    systems: ["HashiCorp Vault / AWS Secrets Manager / Azure Key Vault / CyberArk Conjur", "Secret scanners (gitleaks / TruffleHog) over repos + images", "CI/CD secret stores"],
    owners: ["Platform security / secrets team — owns the vault", "Application owners — own their secrets", "DevOps"],
    tools: [
      "vault inventory: secrets + last-rotated + access policies per workload",
      "gitleaks / TruffleHog scan of repos, CI logs, and container images for embedded secrets",
      "Vault dynamic secrets (DB/cloud) usage vs static; check lease TTLs",
      "review the secret-access audit log (which identity read which secret, when)",
    ],
    finding: "A live database password and an AWS access key are committed in a repo's history; most vault secrets are static and years old; and one CI role can read every secret in the vault.",
    refs: [["OWASP Secrets Management Cheat Sheet", "https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html"], ["NIST SP 800-57", "https://csrc.nist.gov/projects/key-management"], ["CIS Control 16", "https://www.cisecurity.org/controls"]],
  },
  "cks-03": {
    artifacts: [
      "The enterprise certificate inventory (TLS, client, code-signing, device) across internal + external, with CA, key, expiry, deployment",
      "The CA hierarchy + issuance policy + Certificate Policy/CPS",
      "Renewal-automation evidence (ACME / auto-enrol) vs manual, plus the expiry-outage history",
      "Private-key protection — HSM-backed CA keys, key-escrow/recovery policy",
    ],
    test: "Verify the certificate + PKI estate is governed and won't fail. PASS: a complete cert inventory exists; the CA hierarchy is documented with a CP/CPS and HSM-protected CA keys; certs use approved algorithms/key sizes; renewals are automated (no surprise expiries); revocation (CRL/OCSP) works; and code-signing keys are tightly controlled. Exceptions: unknown/expired certs in production, weak keys (RSA-1024/SHA-1), CA keys not in an HSM, manual renewals causing outages, and broadly-accessible code-signing keys.",
    systems: ["Venafi / Keyfactor / Microsoft AD CS / AWS Private CA", "Certificate Transparency logs (discovery)", "Load balancers / servers / device fleets", "HSM (CA + code-signing keys)"],
    owners: ["PKI / Cryptography team — owns the CA", "Platform & application owners — own deployed certs", "Developers (code signing)"],
    tools: [
      "Venafi/Keyfactor inventory export (CN, issuer, key, expiry, location)",
      "Certificate Transparency query (crt.sh) for every cert ever issued for the org's domains — find the unknowns",
      "AD CS issued-cert database + template review (weak-key/EKU issues, key archival)",
      "confirm CA + code-signing private keys are HSM-resident; check OCSP/CRL availability",
    ],
    finding: "Several internet certificates are RSA-1024/SHA-1, the issuing CA's private key sits on a VM disk (not an HSM), code-signing keys are shared on a build server, and three production certs expired last quarter causing outages.",
    refs: [["NIST SP 800-57", "https://csrc.nist.gov/projects/key-management"], ["CA/Browser Forum Baseline Requirements", "https://cabforum.org/baseline-requirements-documents/"], ["NIST SP 1800-16 TLS Certificate Management", "https://www.nccoe.nist.gov/projects/building-blocks/tls-server-certificate-management"]],
  },
  "cks-04": {
    artifacts: [
      "The HSM inventory (model, FIPS 140-2/3 level, firmware) + which keys/operations each protects",
      "HSM access-control + quorum (M-of-N) configuration for administrative operations",
      "HSM audit logs (key use, admin actions) + their export to the SIEM",
      "HSM backup, secure key-cloning, and DR procedures",
    ],
    test: "Verify HSMs protecting critical keys are correctly controlled. PASS: HSMs are FIPS 140-2 Level 3+ (or 140-3) and current on firmware; administrative operations require quorum (M-of-N) authorisation; roles are separated so no single operator can extract/clone keys; all HSM events are logged to the SIEM; and backup/DR (secure key-cloning) is tested. Exceptions: keys protected by software KMS that should be in an HSM, HSM admin without quorum, single-person key control, HSM logs not collected, and no tested HSM DR.",
    systems: ["HSMs — Thales Luna / Entrust nShield / AWS CloudHSM / Azure Dedicated HSM", "The key-management / PKI systems using them", "SIEM (HSM logs)"],
    owners: ["Cryptography / Key Management team — owns the HSMs", "Security operations (monitoring)", "Infrastructure (DR)"],
    tools: [
      "HSM partition/slot inventory + firmware vs vendor advisories; confirm the FIPS level",
      "review the M-of-N quorum configuration for admin operations + role separation",
      "confirm HSM audit logs export to the SIEM (key-use + admin actions)",
      "verify the backup/clone + DR procedure is documented and tested",
    ],
    finding: "The root CA and code-signing keys live in a single HSM with no quorum (one operator can run admin operations), the firmware is two versions behind a security advisory, and HSM logs aren't sent anywhere.",
    refs: [["FIPS 140-3", "https://csrc.nist.gov/pubs/fips/140-3/final"], ["NIST SP 800-57", "https://csrc.nist.gov/projects/key-management"], ["PCI PIN Security / HSM", "https://www.pcisecuritystandards.org/"]],
  },
  "cks-05": {
    artifacts: [
      "The cryptographic standard the org mandates (approved algorithms, key sizes, modes, protocols)",
      "A code/config scan of where crypto is implemented (TLS, app crypto libraries, password/data hashing)",
      "Findings of weak/deprecated crypto in use (MD5/SHA-1, DES/3DES/RC4, ECB mode, hardcoded keys/IVs, weak RNG)",
      "The crypto-library/version inventory + known-vulnerability status",
    ],
    test: "Verify cryptography is implemented per standard and free of weak primitives. PASS: only approved algorithms/modes are used (AES-GCM, SHA-256+, RSA-2048+/ECDSA-P256+, TLS 1.2+); passwords are hashed with a strong KDF (bcrypt/scrypt/Argon2/PBKDF2 high-iteration, salted); IVs/nonces and RNG are correct (CSPRNG); crypto libraries are current. Exceptions: MD5/SHA-1 for security, DES/3DES/RC4, ECB mode, hardcoded keys/IVs, weak/predictable RNG, fast-hash or unsalted password storage, and outdated crypto libraries with CVEs.",
    systems: ["Source code + SAST (Semgrep / CodeQL crypto rules)", "TLS/endpoint scanners (testssl.sh)", "SCA (crypto-library versions)", "Application configuration"],
    owners: ["Application security / developers — implement crypto", "Cryptography team — sets the standard", "AppSec"],
    tools: [
      "SAST crypto rules (CodeQL/Semgrep): MD5/SHA-1, DES/3DES/RC4, ECB, Math.random for security, hardcoded keys/IVs",
      "inspect password hashing: bcrypt/Argon2/PBKDF2 vs unsalted MD5/SHA-1",
      "testssl.sh for protocol/cipher; SCA for crypto-library versions + CVEs",
      "review IV/nonce handling and the RNG source (must be a CSPRNG)",
    ],
    finding: "Passwords are stored as unsalted SHA-1, an internal API encrypts tokens with 3DES in ECB mode under a hardcoded key, and two services generate session tokens with `Math.random()`.",
    refs: [["NIST SP 800-175B Crypto Guidelines", "https://csrc.nist.gov/pubs/sp/800/175/b/r1/final"], ["OWASP Cryptographic Storage Cheat Sheet", "https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html"]],
  },
  "cks-06": {
    artifacts: [
      "The enterprise Cryptographic Bill of Materials (CBOM) — all algorithms, key sizes, protocols, and where used",
      "The list of quantum-vulnerable crypto (RSA, ECC, Diffie-Hellman) in use + the data/secrecy lifetime it protects",
      "The crypto-agility assessment — can algorithms be swapped centrally without re-architecture",
      "The PQC migration roadmap aligned to NIST FIPS 203/204/205 + CNSA 2.0 timelines",
    ],
    test: "Assess enterprise readiness to migrate to post-quantum cryptography. PASS: a CBOM inventories all crypto in use; quantum-vulnerable algorithms are catalogued with the data they protect; crypto-agility lets algorithms be swapped centrally; an HNDL-prioritised migration plan to NIST PQC (ML-KEM / ML-DSA) + hybrid exists with CNSA-2.0-aligned timelines; and vendor PQC roadmaps are tracked. Exceptions: no CBOM, no agility (hardcoded crypto), long-lived secrets on classical algorithms, no migration plan, and unknown vendor PQC posture.",
    systems: ["CBOM tooling (CycloneDX crypto-assets, Venafi/Keyfactor)", "The TLS + PKI + KMS estate", "Vendor roadmaps"],
    owners: ["Cryptography / PKI team — owns the migration", "Enterprise architecture — owns agility", "Vendor management"],
    tools: [
      "generate a CBOM (CycloneDX crypto-assets) across the estate",
      "inventory RSA/ECC/DH usage + the data secrecy-lifetime each protects (HNDL prioritisation)",
      "assess crypto-agility: is crypto behind a central abstraction or hardcoded per app?",
      "map current → NIST PQC (FIPS 203/204) + hybrid; track vendor PQC commitments",
    ],
    finding: "No CBOM exists; RSA-2048 and P-256 are everywhere with no central abstraction (each app hardcodes its crypto), long-secrecy data rides classical key-exchange, and no key vendor has published a PQC date.",
    refs: [["NIST FIPS 203/204/205 (PQC)", "https://csrc.nist.gov/projects/post-quantum-cryptography"], ["NSA CNSA 2.0", "https://www.nsa.gov/Cybersecurity/Post-Quantum-Cybersecurity-Resources/"], ["NIST SP 1800-38 PQC Migration", "https://www.nccoe.nist.gov/crypto-agility-considerations-migrating-post-quantum-cryptographic-algorithms"]],
  },
  "cks-07": {
    artifacts: [
      "The secret-scanning coverage across repos, CI/CD, container images, and IaC (which are scanned)",
      "Pre-commit / push-protection configuration (secrets blocked before they land)",
      "The historical-secret findings + their remediation (rotated, not just deleted)",
      "The allow-list / false-positive management + the detector ruleset",
    ],
    test: "Verify secrets are detected and prevented from entering code/config. PASS: secret scanning runs across all repos (history + new commits), CI logs, container images, and IaC; push-protection / pre-commit blocks new secrets before merge; detected secrets are rotated (not merely removed from the latest commit); and coverage is near-complete. Exceptions: repos/images not scanned, no push-protection (secrets land then are 'deleted' but stay in history and stay valid), found secrets removed but never rotated, and scanning whose findings no one actions.",
    systems: ["GitHub Advanced Security / GitLab secret scanning / gitleaks / TruffleHog", "Pre-commit hooks + CI scanning + push protection", "The vault (where secrets should live instead)"],
    owners: ["AppSec / platform security — own scanning", "Developers — remediate", "DevOps"],
    tools: [
      "gitleaks / TruffleHog across all repos INCLUDING full git history; scan images for embedded secrets",
      "confirm push-protection / pre-commit secret blocking is enabled org-wide",
      "for each historical finding, confirm the secret was ROTATED (credential invalidated), not just deleted",
      "scan CI/CD logs + IaC variable files (tfvars) for secrets",
    ],
    finding: "Push-protection is off, so secrets routinely land and get 'fixed' by a follow-up commit — but the credentials in history are still valid; a history scan surfaces 40 live keys nobody rotated.",
    refs: [["OWASP Secrets Management Cheat Sheet", "https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html"], ["GitHub Secret Scanning", "https://docs.github.com/code-security/secret-scanning"]],
  },
  "cks-08": {
    artifacts: [
      "The logging configuration for key/secret/certificate systems (KMS, Vault, HSM, CA) — what is logged",
      "Forwarding of those logs to the SIEM + detections (key-use anomalies, mass secret reads, cert issuance, CA admin)",
      "Retention + tamper-protection of crypto-system logs",
      "Review evidence — who watches privileged key/secret access",
    ],
    test: "Verify cryptographic-system activity is logged and monitored. PASS: KMS, Vault, HSM, and CA all log key use, secret access, certificate issuance, and admin actions; logs ship to the SIEM with detections for anomalies (mass secret reads, unusual key-use, unexpected cert issuance, CA admin); logs are retained and tamper-protected; and privileged access is reviewed. Exceptions: crypto systems not logging, logs not forwarded, no detections on key/secret abuse, short retention, and unmonitored CA/HSM admin actions.",
    systems: ["KMS / Vault / HSM / CA log sources", "SIEM (Sentinel / Splunk)", "Detection content"],
    owners: ["Cryptography team + security operations", "SOC", "Detection engineering"],
    tools: [
      "confirm CloudTrail KMS data events, the Vault audit device, HSM logs, and CA issuance logs are all on",
      "verify all forward to the SIEM; check retention",
      "detections present for: mass secret reads, Decrypt spikes, unexpected cert issuance, CA/HSM admin actions",
      "review privileged key/secret access (who, what, reviewed by whom)",
    ],
    finding: "KMS Decrypt data-events aren't logged, the Vault audit device is disabled, and CA issuance has no monitoring — so an attacker minting certificates or bulk-decrypting data would be invisible.",
    refs: [["NIST SP 800-92 Log Management", "https://csrc.nist.gov/pubs/sp/800/92/final"], ["PCI DSS Req. 10", "https://www.pcisecuritystandards.org/"]],
  },
  "cks-09": {
    artifacts: [
      "The mapping of the org's cryptography to applicable mandates (FIPS 140-3, PCI DSS, GDPR, HIPAA, CNSA, eIDAS)",
      "Validation evidence — FIPS-validated modules used where required (CMVP certificate numbers)",
      "The gap assessment vs each mandate's crypto requirements + remediation plan",
      "Records demonstrating crypto compliance for audits/regulators",
    ],
    test: "Verify the org's cryptography meets its regulatory + contractual obligations. PASS: applicable crypto mandates are identified; FIPS-validated modules (CMVP-listed) are used where required; algorithms/key sizes/protocols meet each standard (e.g. PCI strong crypto, CNSA for national-security systems); and compliance is evidenced for audit. Exceptions: non-FIPS-validated crypto where validation is required, algorithms below a mandate's floor, missing CMVP evidence, and crypto-compliance gaps with no remediation plan.",
    systems: ["The crypto inventory / CBOM", "CMVP (the FIPS-validated module list)", "GRC (mandate mapping + evidence)"],
    owners: ["Cryptography team + Compliance / GRC", "Legal", "Security"],
    tools: [
      "map each system's crypto to applicable mandates (FIPS 140-3, PCI, HIPAA, GDPR, CNSA, eIDAS)",
      "verify modules are CMVP-validated where required (check the certificate numbers on the NIST CMVP list)",
      "gap-assess algorithms/key sizes against each mandate's floor",
      "assemble the crypto-compliance evidence pack for audit",
    ],
    finding: "A FedRAMP system uses a non-FIPS-validated crypto library for data-at-rest (validation is mandatory there), and several PCI-scoped services still permit TLS 1.0 — both reportable compliance gaps with no remediation plan.",
    refs: [["FIPS 140-3 / CMVP", "https://csrc.nist.gov/projects/cryptographic-module-validation-program"], ["PCI DSS", "https://www.pcisecuritystandards.org/"], ["NSA CNSA 2.0", "https://www.nsa.gov/Cybersecurity/Post-Quantum-Cybersecurity-Resources/"]],
  },

  // ── Endpoint Devices ────────────────────────────────────────────────────────
  "ept-01": {
    artifacts: ["The MDM/UEM enrolment report — devices enrolled + managed vs the total known device population", "The configuration-profile baseline pushed to managed devices (encryption, screen-lock, patch, EDR)", "Per-device compliance status against the baseline", "The unmanaged/unenrolled device list"],
    test: "Verify all corporate endpoints are enrolled in and governed by the management platform. PASS: every in-scope device (Windows/macOS/mobile) is enrolled in MDM/UEM, receives the security baseline, and reports compliant; unmanaged devices are blocked from corporate resources. Exceptions: devices not enrolled, enrolled-but-non-compliant devices, and unmanaged devices with access to corporate data.",
    systems: ["Microsoft Intune / Jamf Pro / VMware Workspace ONE (MDM/UEM)", "The IdP (conditional access on device compliance)", "Asset inventory / CMDB (the denominator)"],
    owners: ["End-user computing / IT — owns the platform", "Security — owns the baseline", "Asset management"],
    tools: ["Intune/Jamf: enrolled + compliant device count vs the CMDB/AD device count (the gap)", "export the configuration-profile baseline + per-device compliance state", "Conditional Access: is device-compliance required for corporate apps?", "list devices with corporate access that are NOT enrolled"],
    finding: "Intune covers Windows but 200 macOS and most personal mobiles accessing email are unmanaged; 15% of enrolled devices report non-compliant (encryption or patch) yet still have access.",
    refs: [["NIST SP 800-124 Mobile Device Security", "https://csrc.nist.gov/pubs/sp/800/124/r2/final"], ["CIS Control 1 / 4", "https://www.cisecurity.org/controls"]],
  },
  "ept-02": {
    artifacts: ["The authoritative endpoint inventory reconciled from MDM + EDR + AD + DHCP/NAC", "Discovery of devices on the network NOT in the inventory (rogue/unauthorised)", "The reconciliation gaps between sources (in EDR but not MDM, on network but in neither)", "The process to detect + quarantine unauthorised devices"],
    test: "Verify the org has a complete, reconciled endpoint inventory and detects unauthorised devices. PASS: a single authoritative inventory reconciles MDM, EDR, directory, and network sources; devices on the network are matched to it; and unknown/unauthorised devices are detected and quarantined (NAC). Exceptions: large reconciliation gaps, rogue devices on the network with no detection, and no NAC/quarantine for unknowns.",
    systems: ["MDM + EDR + AD + DHCP/NAC (the sources)", "NAC — Cisco ISE / Aruba ClearPass", "Asset discovery — runZero / Lansweeper"],
    owners: ["IT asset management — owns the inventory", "Network security (NAC)", "Security operations"],
    tools: ["reconcile device lists: AD computers ∪ EDR ∪ MDM ∪ DHCP leases → find the gaps", "runZero / Lansweeper network discovery vs the inventory (rogue devices)", "NAC logs: unknown devices that connected — were they quarantined?", "count endpoints with EDR but no MDM (and vice-versa)"],
    finding: "EDR sees 4,200 endpoints, MDM 3,600, AD 5,000 — none reconcile; network discovery finds 300 devices in no inventory, and there's no NAC to stop an unknown laptop from joining.",
    refs: [["CIS Control 1 Inventory of Enterprise Assets", "https://www.cisecurity.org/controls"], ["NIST SP 800-128", "https://csrc.nist.gov/pubs/sp/800/128/final"]],
  },
  "ept-03": {
    artifacts: ["The hardware asset register (procurement → deployment → refresh → disposal) with owner, location, status", "Reconciliation of the register against deployed/discovered devices", "Disposal/decommission records (secure wipe / certificate of destruction)", "Lost/stolen device records + their handling"],
    test: "Verify hardware is tracked across its full lifecycle. PASS: every device has a register entry through procurement, assignment, refresh, and disposal; the register reconciles to discovered devices; disposed devices were securely wiped (NIST 800-88) with certificates; and lost/stolen devices are recorded and remotely wiped. Exceptions: deployed devices not in the register (or vice-versa), disposed devices with no wipe evidence, and lost devices not remote-wiped.",
    systems: ["ITAM — ServiceNow ITAM / Lansweeper / Snipe-IT", "MDM (remote wipe)", "Disposal-vendor records"],
    owners: ["IT asset management — owns the register", "End-user computing", "Procurement / disposal"],
    tools: ["reconcile the ITAM register vs MDM + EDR discovered devices", "disposal: certificates of destruction / crypto-erase logs per NIST 800-88", "lost/stolen log: confirm remote wipe was issued AND confirmed", "check for register entries stuck 'in stock' that are actually deployed"],
    finding: "The asset register is ~70% accurate; 50 'disposed' laptops have no wipe certificate (data may have left on resold drives), and two reported-lost laptops were never remote-wiped.",
    refs: [["NIST SP 800-88 Media Sanitisation", "https://csrc.nist.gov/pubs/sp/800/88/r1/final"], ["ISO/IEC 27001 A.5.9 / A.7.14", "https://www.iso.org/standard/27001"]],
  },
  "ept-04": {
    artifacts: ["The approved gold-image/build definitions + versions per OS", "The deployed-image-version distribution across the fleet", "The image-build hardening + patch-currency at bake time", "Devices running outdated/unapproved images"],
    test: "Verify endpoints run a current, approved, hardened image. PASS: gold images are versioned, hardened (CIS), and patched at bake; the fleet runs current image versions; and re-imaging/refresh keeps drift bounded. Exceptions: devices on images years old, unapproved/unmanaged builds, images that ship unpatched or unhardened, and no process to refresh stale images.",
    systems: ["Imaging — Autopilot / MDT-SCCM / Jamf (the gold-image pipeline)", "MDM (deployed image/build version)", "CIS-CAT (image hardening)"],
    owners: ["End-user computing / engineering — owns images", "Security — owns hardening", "IT operations"],
    tools: ["MDM: deployed OS build + image-version distribution across the fleet", "compare to the approved gold-image versions", "CIS-CAT the gold image to confirm hardening + patch currency at bake", "flag devices on deprecated/unapproved builds"],
    finding: "A third of the fleet runs a gold image from two years ago with known-unpatched components baked in, and several departments use their own unapproved, unhardened images.",
    refs: [["CIS Benchmarks", "https://www.cisecurity.org/cis-benchmarks"], ["NIST SP 800-128", "https://csrc.nist.gov/pubs/sp/800/128/final"]],
  },
  "ept-05": {
    artifacts: ["The EDR/AV deployment report — agents installed + healthy + reporting vs the device inventory", "EDR policy config (real-time protection, tamper protection, prevention vs detect-only, cloud lookups)", "Detection/response coverage + recent blocked-threat evidence", "Agent-health gaps (disabled, outdated, not reporting)"],
    test: "Verify EDR/antimalware is deployed, healthy, and in prevention mode everywhere. PASS: EDR agents are on ~100% of endpoints, healthy and reporting; tamper protection is on; policy is in prevention (block) not detect-only; sensors/signatures are current; and detections route to the SOC. Exceptions: endpoints with no EDR or a disabled/unhealthy agent, detect-only mode on production, tamper protection off, and stale agents.",
    systems: ["EDR — CrowdStrike Falcon / Microsoft Defender for Endpoint / SentinelOne", "The device inventory (coverage denominator)", "SIEM / SOC"],
    owners: ["Security operations — owns EDR", "End-user computing (deploys agents)", "SOC"],
    tools: ["EDR console: healthy reporting agents vs the device inventory (coverage gap)", "policy export: prevention vs detect-only, tamper protection, real-time on", "agent-version / sensor-currency report; list disabled/unhealthy agents", "sample recent prevented detections"],
    finding: "EDR covers 85% of endpoints; 300 have no agent and 120 have it disabled or not reporting; and the server policy is detect-only, so malware would be seen but not blocked.",
    refs: [["CIS Control 10 Malware Defenses", "https://www.cisecurity.org/controls"], ["MITRE ATT&CK", "https://attack.mitre.org/"]],
  },
  "ept-06": {
    artifacts: ["The cross-control coverage matrix per device (MDM, EDR, encryption, patch, logging — ✓/✗ each)", "The list of devices missing one or more required controls", "The systemic-gap analysis (which control, which population, why)", "The remediation/onboarding plan for the gaps"],
    test: "Verify every endpoint has the full required control stack, with gaps found and closed. PASS: a per-device coverage matrix reconciles MDM, EDR, encryption, patch, and logging; near-100% of devices have all required controls; and any device missing a control is detected and remediated. Exceptions: devices missing one or more controls (EDR-but-no-encryption, encrypted-but-no-logging), systemic populations excluded (servers, OT, contractor devices), and no process to find gaps.",
    systems: ["MDM + EDR + encryption + patch + SIEM (joined)", "The reconciliation/coverage tooling or a manual join", "CMDB"],
    owners: ["Security operations / endpoint security — owns coverage", "IT", "Asset management"],
    tools: ["join device lists across MDM, EDR, BitLocker/FileVault, patch, and SIEM into one matrix", "flag every device missing ≥1 control; group by the missing control + population", "find populations systematically excluded (servers, lab machines, BYOD)", "track gaps to closure"],
    finding: "When the controls are joined per device, only 62% have the full stack; a whole population of lab machines has EDR but no encryption or logging, and contractor laptops have none of it.",
    refs: [["CIS Critical Security Controls", "https://www.cisecurity.org/controls"], ["NIST Cybersecurity Framework", "https://www.nist.gov/cyberframework"]],
  },
  "ept-07": {
    artifacts: ["The endpoint patch-compliance report (missing OS + third-party patches, severity, age) per device", "The endpoint patch SLA/cadence policy", "The patch-ring/deployment config (rings, deferral, reboot enforcement)", "Devices stuck behind on patches (failed/paused updates)"],
    test: "Verify endpoints are patched within cadence (OS + common third-party apps). PASS: a patch policy with severity SLAs exists; ~100% of endpoints are within SLA for OS and high-risk third-party apps (browsers, Java, Adobe); deployment rings + reboot enforcement keep devices current; and KEV-listed endpoint CVEs are remediated. Exceptions: endpoints months behind, third-party apps unpatched, devices with paused/failed updates, and no third-party patching at all.",
    systems: ["Intune / WSUS-SCCM / Jamf + third-party patching (Patch My PC, Autopkg)", "Vuln scanner (missing-patch view)", "CISA KEV"],
    owners: ["End-user computing / IT operations — patch", "Security operations — track SLA", "Risk (exceptions)"],
    tools: ["Intune/SCCM update-compliance per device; identify devices behind SLA", "third-party patch coverage (browsers/Java/Adobe) — usually the real gap", "list devices with paused/failed update history", "join endpoint CVEs to the CISA KEV catalogue"],
    finding: "OS patching is decent but third-party apps aren't managed at all — most endpoints run outdated Chrome/Acrobat with KEV-listed CVEs, and 8% of devices have updates paused indefinitely.",
    refs: [["NIST SP 800-40 Patch Management", "https://csrc.nist.gov/pubs/sp/800/40/r4/final"], ["CISA KEV", "https://www.cisa.gov/known-exploited-vulnerabilities-catalog"]],
  },
  "ept-08": {
    artifacts: ["Authenticated endpoint vuln-scan results (or EDR-native vuln data) across the fleet", "Scan/assessment coverage vs the device inventory", "Risk-prioritised endpoint findings (severity + KEV + exploitability) + remediation tracking", "Misconfiguration findings (weak configs, not just missing patches)"],
    test: "Verify endpoints are assessed for vulnerabilities + misconfigurations. PASS: authenticated scanning or EDR-native vuln management covers ~100% of endpoints; findings cover missing patches AND misconfigurations; they're risk-prioritised (KEV/exploitability) and remediated within SLA; coverage spans roaming/remote devices. Exceptions: large scan-coverage gaps (esp. remote devices unreachable by network scanners), unauthenticated scans missing host issues, config issues ignored, and findings never remediated.",
    systems: ["Tenable/Qualys/Rapid7 or EDR-native (Defender Vulnerability Management, CrowdStrike Spotlight)", "The device inventory", "CISA KEV / exploitability feeds"],
    owners: ["Vulnerability management / endpoint security", "IT operations (remediate)", "Risk"],
    tools: ["EDR-native vuln management (Defender TVM / CrowdStrike Spotlight) for roaming-device coverage", "scan coverage vs device inventory; flag unreachable/remote devices", "prioritise by KEV + exploitability, not raw CVSS", "include misconfiguration findings (secure-config gaps), not just patches"],
    finding: "Network-based scanning misses all remote/roaming laptops (40% of the fleet), and where it does run it's unauthenticated — so most host vulnerabilities and all misconfigurations are invisible.",
    refs: [["NIST SP 800-40", "https://csrc.nist.gov/pubs/sp/800/40/r4/final"], ["CIS Control 7", "https://www.cisecurity.org/controls"]],
  },
  "ept-09": {
    artifacts: ["The inventory of OS/hardware versions vs vendor end-of-support dates", "The list of endpoints running EOL/unsupported OS (no more security patches)", "The EOL-transition/refresh plan + isolation for EOL devices that must remain", "Extended-support (ESU) coverage where applicable"],
    test: "Verify no endpoint runs unsupported software/hardware that no longer receives security patches. PASS: OS + hardware are mapped to vendor EOL dates; devices are refreshed/upgraded before EOL; any EOL device that must remain is isolated, compensated, and ESU-covered. Exceptions: endpoints on EOL OS receiving no security patches, no refresh plan, and EOL devices on the production network with no isolation.",
    systems: ["MDM / inventory (OS + hardware versions)", "Vendor EOL calendars", "Network isolation (for unavoidable EOL)"],
    owners: ["End-user computing / IT — owns refresh", "Security (isolation)", "Asset management"],
    tools: ["map deployed OS/hardware versions to vendor end-of-support dates", "list endpoints on EOL OS (e.g. Windows 10 after Oct 2025 with no ESU)", "confirm a funded refresh plan; check ESU enrolment where used", "verify any remaining EOL devices are network-isolated + compensated"],
    finding: "200 endpoints still run an EOL OS with no ESU (zero security patches), they sit on the general network with no isolation, and there's no funded refresh plan.",
    refs: [["Microsoft / vendor lifecycle policies", "https://learn.microsoft.com/lifecycle/"], ["CIS Control 2 Software Assets", "https://www.cisecurity.org/controls"]],
  },
  "ept-10": {
    artifacts: ["The removable-media + peripheral-control policy and its MDM enforcement (USB storage block/encrypt, Bluetooth restrictions)", "The device-control config across the fleet (allow-list of approved peripherals)", "Logs of USB/peripheral usage + blocked attempts", "BadUSB / DMA-attack mitigations (Thunderbolt/DMA protection)"],
    test: "Verify peripheral and removable-media controls are enforced. PASS: USB mass-storage is blocked or forced-encrypted per policy; only approved peripherals are allowed (device-control allow-list); Bluetooth is restricted/managed; DMA/Thunderbolt protection is on; and usage is logged. Exceptions: unrestricted USB storage (data-exfil + malware vector), no peripheral allow-list, open Bluetooth, DMA protection off, and no logging of removable-media use.",
    systems: ["Intune/Jamf device control / Microsoft Defender device control / endpoint DLP", "BitLocker To Go (forced encryption)", "SIEM (USB logs)"],
    owners: ["Endpoint security — owns device control", "IT", "Data protection"],
    tools: ["MDM device-control policy: USB-storage block/encrypt + peripheral allow-list config", "confirm BitLocker To Go (or block) on removable storage", "Defender device-control + Kernel DMA Protection status", "SIEM: USB-insertion + blocked-peripheral events"],
    finding: "USB mass storage is fully open across the fleet with no encryption requirement and no logging — a contractor copied the customer database to a thumb drive and nothing recorded or blocked it; Bluetooth is unrestricted.",
    refs: [["CIS Control 10", "https://www.cisecurity.org/controls"], ["NIST SP 800-53 MP / SC families", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"]],
  },
  "ept-11": {
    artifacts: ["The screen-lock/timeout policy (inactivity timeout, password/biometric on resume) and its MDM enforcement", "Per-device compliance with the lock policy", "The lock policy for privileged/kiosk/shared devices", "Evidence the policy can't be disabled locally"],
    test: "Verify endpoints auto-lock after inactivity and require re-authentication. PASS: an inactivity timeout (≤15 min, shorter for privileged/admin) is enforced via MDM and can't be disabled locally; resume requires password/biometric; and kiosk/shared devices have appropriate session handling. Exceptions: no timeout or an excessive one (hours), users able to disable the lock, and privileged workstations with the same long timeout as standard devices.",
    systems: ["Intune/Jamf configuration profiles (screen-lock policy)", "AD GPO (legacy)", "MDM compliance reporting"],
    owners: ["Endpoint security / IT — owns the policy", "Security (privileged-device policy)"],
    tools: ["MDM: screen-lock/timeout profile config + per-device compliance", "confirm the setting is managed (greyed-out for the user, not user-editable)", "check privileged/admin workstations have a shorter timeout", "review kiosk/shared-device session policy"],
    finding: "The inactivity-lock policy exists but isn't enforced via MDM, so many users set it to 'never'; privileged admin workstations use the same 30-minute timeout as everyone else.",
    refs: [["CIS Control 4", "https://www.cisecurity.org/controls"], ["NIST SP 800-53 AC-11 Session Lock", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"]],
  },
  "ept-12": {
    artifacts: ["The full-disk-encryption status per device (BitLocker/FileVault on, fully encrypted, correct cipher)", "Encryption coverage vs the device inventory", "Recovery-key escrow evidence (keys escrowed to MDM/AD, recoverable)", "Removable + external-drive encryption posture"],
    test: "Verify endpoints are fully-disk-encrypted with escrowed recovery keys. PASS: ~100% of endpoints have FDE enabled and completed (BitLocker XTS-AES, FileVault) with TPM-backed protectors; recovery keys are escrowed (Intune/AD) and recoverable; and removable drives are encrypted. Exceptions: devices with encryption off or suspended, decryption-paused devices, recovery keys not escrowed (data unrecoverable + audit fail), and unencrypted external drives.",
    systems: ["BitLocker (Windows) / FileVault (macOS) via MDM", "Intune/Jamf (key escrow + status)", "Device inventory"],
    owners: ["Endpoint security / IT — owns encryption", "Security", "Service desk (recovery)"],
    tools: ["MDM: FDE status (encrypted/encrypting/off) per device vs the inventory", "confirm recovery-key escrow to Intune/AD (and test a recovery)", "BitLocker: TPM + cipher (XTS-AES-256) + protector config", "removable-drive encryption (BitLocker To Go) status"],
    finding: "12% of laptops are unencrypted or have BitLocker suspended; for several encrypted devices the recovery key was never escrowed — so a lost laptop is both a breach and unrecoverable.",
    refs: [["CIS Control 3 Data Protection", "https://www.cisecurity.org/controls"], ["NIST SP 800-111 Storage Encryption", "https://csrc.nist.gov/pubs/sp/800/111/final"]],
  },
  "ept-13": {
    artifacts: ["The endpoint-DLP policy + coverage (sensitive-data detection on copy/print/upload/USB)", "Enrolled endpoints vs total", "Endpoint-DLP incident logs + disposition", "Uncovered exfil channels on the endpoint (clipboard, screen capture, personal-sync clients)"],
    test: "Verify endpoint DLP detects + controls sensitive-data egress at the device. PASS: endpoint DLP covers the fleet, detects sensitive types on USB/print/clipboard/upload/personal-sync, blocks or warns per policy, and feeds incidents to the SOC. Exceptions: endpoints without the DLP agent, key channels uncovered (USB, personal cloud sync, clipboard), incidents not triaged, and trivial override.",
    systems: ["Microsoft Purview Endpoint DLP / Forcepoint / Digital Guardian", "The endpoint agent + the device inventory", "SIEM"],
    owners: ["Data protection / endpoint security", "Privacy (sensitive types)", "SOC"],
    tools: ["endpoint-DLP console: enrolled endpoints vs inventory + policy per channel", "test channels: USB copy, print, personal OneDrive/Drive sync, clipboard to webmail", "incident export + disposition", "confirm personal-sync-client + clipboard controls"],
    finding: "Endpoint DLP runs on 40% of devices and doesn't cover personal cloud-sync clients, so customer files sync to a personal OneDrive uncaught; incidents that do fire go to an unmonitored queue.",
    refs: [["CIS Control 3", "https://www.cisecurity.org/controls"], ["Microsoft Purview Endpoint DLP", "https://learn.microsoft.com/purview/endpoint-dlp-learn-about"]],
  },
  "ept-14": {
    artifacts: ["The mobile-device + BYOD inventory and management model (MDM-managed vs MAM/containerised)", "The container/app-protection policy (work-data isolation, copy/paste restriction, selective wipe, no corp-data backup)", "Compliance + jailbreak/root detection status", "Conditional access tying mobile compliance to corporate access"],
    test: "Verify mobile + BYOD access to corporate data is managed and isolated. PASS: corporate mobiles are enrolled with a security baseline; BYOD uses MAM/containerisation that isolates work data (no copy to personal apps, selective wipe, encrypted container); jailbroken/rooted devices are blocked; and conditional access requires compliance. Exceptions: unmanaged mobiles with corporate mail/data, BYOD with no container (corp data in personal apps), no jailbreak detection, and no compliance gate.",
    systems: ["Intune (MDM + App Protection / MAM) / Jamf / Workspace ONE", "IdP conditional access", "Mobile inventory"],
    owners: ["Endpoint security / mobility — owns MDM/MAM", "IAM (conditional access)", "IT"],
    tools: ["Intune: managed-device + App-Protection-Policy coverage; jailbreak/root compliance", "confirm MAM policy: work-data encryption, copy/paste restriction, selective wipe, block backup", "Conditional Access requiring a compliant / MAM-protected app for mobile mail", "list mobiles with corporate access but no management"],
    finding: "Personal phones access corporate email with no app-protection policy, so customer data sits unencrypted in the native mail app and can be copied anywhere; jailbroken devices aren't detected or blocked.",
    refs: [["NIST SP 800-124", "https://csrc.nist.gov/pubs/sp/800/124/r2/final"], ["OWASP MASVS", "https://mas.owasp.org/MASVS/"]],
  },
  "ept-15": {
    artifacts: ["The network-access-control posture policy (device must be managed + compliant to reach the network/apps)", "NAC enforcement coverage (wired + wireless) and what happens to non-compliant/unknown devices", "VPN/ZTNA device-posture checks before access", "The quarantine/remediation VLAN config + bypass exceptions"],
    test: "Verify network/app access requires a healthy, authorised device (posture-based access). PASS: NAC authenticates + posture-checks devices on wired and wireless (802.1X), quarantining unknown/non-compliant devices; VPN/ZTNA enforce device posture (managed + compliant) before access; bypass exceptions are minimal and tracked. Exceptions: open ports/SSIDs with no NAC, non-compliant devices reaching production, VPN/ZTNA with no posture check, and broad MAC-bypass exceptions.",
    systems: ["NAC — Cisco ISE / Aruba ClearPass (802.1X)", "VPN/ZTNA posture (AnyConnect/GlobalProtect HIP, ZTNA)", "MDM (the compliance signal)"],
    owners: ["Network security (NAC/ZTNA)", "Endpoint security (posture)", "IAM"],
    tools: ["NAC: 802.1X coverage on wired+wireless; policy for unknown/non-compliant → quarantine", "VPN/ZTNA posture config (managed + compliant + AV/patch checks)", "review the MAC-Auth-Bypass exception list (the usual hole)", "test: connect an unmanaged device — does NAC quarantine it?"],
    finding: "Wired ports have no 802.1X, so any device that plugs in gets full network access; the VPN checks credentials but not device posture, and the MAB bypass list has grown to hundreds of un-reviewed entries.",
    refs: [["NIST SP 800-207 Zero Trust", "https://csrc.nist.gov/pubs/sp/800/207/final"], ["CIS Control 12 / 13", "https://www.cisecurity.org/controls"]],
  },
  "ept-16": {
    artifacts: ["Endpoint log/telemetry forwarding coverage to the SIEM (which endpoints send security logs)", "The endpoint log sources enabled (auth, process-creation/4688, PowerShell 4104, Sysmon, EDR telemetry)", "Device-location/GPS capability for lost-device tracking", "Retention of endpoint logs + offline-device log capture"],
    test: "Verify endpoints emit and ship security telemetry, and lost devices can be located. PASS: required endpoint log sources are enabled and ~100% of endpoints forward to the SIEM (incl. roaming via cloud collectors); EDR telemetry is centralised; device location/GPS is available for lost-device recovery; and logs are retained. Exceptions: endpoints not forwarding (investigation blind spots), missing sources (no 4688/Sysmon), roaming devices uncollected, and no location capability for lost devices.",
    systems: ["SIEM + cloud log collectors (for roaming)", "Sysmon / Windows audit / EDR telemetry", "MDM (device location)"],
    owners: ["Security operations / detection engineering", "Endpoint (agents)", "IT (location)"],
    tools: ["SIEM: endpoints reporting in last 24h vs the device inventory (forwarding coverage)", "confirm 4688 + command-line + 4104 script-block + Sysmon enabled fleet-wide", "roaming-device telemetry via cloud collector (not just on-LAN)", "MDM device-location / Find-My for lost-device recovery"],
    finding: "Only on-LAN devices forward logs; the 40% of the fleet that's remote sends nothing, command-line auditing is off, and there's no device-location capability — a lost laptop can't be located or reliably wiped.",
    refs: [["NIST SP 800-92", "https://csrc.nist.gov/pubs/sp/800/92/final"], ["CIS Control 8 Audit Log Management", "https://www.cisecurity.org/controls"]],
  },
  "ept-17": {
    artifacts: ["The endpoint forensic-capability evidence (EDR retains process/file/network history; remote triage/collection available)", "Telemetry-retention sufficient for DFIR (enough history to investigate a past intrusion)", "The endpoint IR/triage playbook + host-isolation capability (network-contain a host from the console)", "Chain-of-custody + evidence-collection process for endpoints"],
    test: "Verify the org could investigate an endpoint compromise. PASS: EDR retains sufficient process/file/network history for DFIR; remote triage/collection (memory, artifacts) is available without physical access; hosts can be network-isolated from the console; and there's a documented forensic process with chain-of-custody. Exceptions: telemetry retention too short to investigate (days, not months), no remote-collection capability, no host-isolation, and no forensic process.",
    systems: ["EDR (forensic timeline + remote response — CrowdStrike RTR / Defender Live Response)", "Forensic tooling (Velociraptor, KAPE)", "Evidence store + case management"],
    owners: ["DFIR / security operations", "SOC", "Legal (chain-of-custody)"],
    tools: ["EDR: telemetry-retention window (enough to investigate a 3-month-old intrusion?)", "confirm remote triage/collection (RTR / Live Response / Velociraptor) is available + tested", "confirm one-click host network-isolation from the EDR console", "review the endpoint forensic + chain-of-custody process"],
    finding: "EDR retains only 7 days of telemetry, so a breach discovered weeks later can't be reconstructed; there's no remote-collection tooling, and analysts can't isolate a compromised host without physically retrieving it.",
    refs: [["NIST SP 800-86 Forensic Techniques", "https://csrc.nist.gov/pubs/sp/800/86/final"], ["NIST SP 800-61", "https://csrc.nist.gov/pubs/sp/800/61/r2/final"]],
  },
  "ept-18": {
    artifacts: ["The endpoint-control exception register (devices/users exempted from EDR, encryption, patch, USB, etc.)", "Each exception's justification, compensating control, owner, approval, and expiry", "Reconciliation of granted exceptions vs actual device state", "Aged/expired/unjustified exceptions"],
    test: "Verify exceptions to endpoint controls are governed, compensated, and time-boxed. PASS: every exemption (no EDR, encryption off, patch deferral, USB allowed) is in a register with a business justification, an approved compensating control, a named owner, and an expiry; exceptions are reviewed and reconciled to reality; expired ones are closed. Exceptions: undocumented exemptions (devices just missing controls with no record), exceptions with no compensating control, no expiry ('permanent temporary' exceptions), and a register that doesn't match the actual exempted devices.",
    systems: ["GRC / exception register (ServiceNow GRC, Archer)", "MDM/EDR (the actual exempted device state)", "Risk management"],
    owners: ["Security / GRC — owns the exception process", "Device/business owners — request + own", "Risk (approves)"],
    tools: ["pull the exception register: justification, compensating control, owner, approval, expiry per exemption", "reconcile the register vs reality (MDM/EDR devices actually missing controls)", "flag exceptions past expiry or with no compensating control", "find undocumented exemptions — exempted devices not in the register"],
    finding: "The exception register lists 30 devices but EDR/MDM show 300 devices missing required controls — most exemptions are undocumented; of those recorded, half have no expiry and no compensating control.",
    refs: [["NIST SP 800-53 (risk acceptance / PM-4)", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["ISO/IEC 27001", "https://www.iso.org/standard/27001"]],
  },

  // ── Build Environment & CI/CD ───────────────────────────────────────────────
  "bcd-01": {
    artifacts: ["The inventory of build agents/runners (self-hosted + cloud) + their hardening + isolation", "The build-agent access model (who/what can run jobs; ephemeral vs persistent runners)", "Network egress + secret exposure of the build environment", "Evidence builds run from reviewed source on isolated, ephemeral runners + produce provenance"],
    test: "Verify the build environment is hardened, isolated, and tamper-resistant (the SolarWinds lesson). PASS: build runners are ephemeral (fresh per job), hardened, network-egress-restricted, and isolated from production; only reviewed source builds; the environment can't be modified to inject artifacts; and build provenance is attested. Exceptions: persistent/shared runners that accumulate state, broad egress, build agents with production access, and no provenance on artifacts.",
    systems: ["CI runners (GitHub Actions runners, GitLab runners, Jenkins agents)", "The build orchestrator", "SLSA / provenance attestation"],
    owners: ["Platform / DevOps engineering — owns build infra", "AppSec — owns build integrity", "Security"],
    tools: ["inventory runners: ephemeral vs persistent, self-hosted vs hosted, who can target them", "check runner egress + whether secrets are exposed to untrusted PR builds", "confirm builds produce SLSA provenance attestation", "verify the runner image is hardened + rebuilt regularly"],
    finding: "Self-hosted runners are persistent and shared across repos with broad egress, so a malicious PR can run on them and exfiltrate other repos' secrets — the SolarWinds-class build-injection risk.",
    refs: [["SLSA", "https://slsa.dev/"], ["NIST SP 800-204D CI/CD Security", "https://csrc.nist.gov/pubs/sp/800/204/d/final"]],
  },
  "bcd-02": {
    artifacts: ["The pipeline-definition inventory + who can edit pipeline config (pipeline-as-code permissions)", "The required security gates in the pipeline (SAST/SCA/secret/image scan) + whether they block or warn", "Branch/environment protection for deploy stages (approvals before prod deploy)", "Pipeline credential scope (deploy creds — OIDC vs long-lived)"],
    test: "Verify the CI/CD pipeline enforces security and can't be subverted. PASS: pipeline definitions are protected (changes reviewed); security gates are blocking, not advisory; production deploys require approval and come only from protected branches; and deploy credentials are short-lived (OIDC federation), least-privilege. Exceptions: anyone can edit the pipeline, gates set to non-blocking, prod deploy with no approval, and long-lived powerful deploy credentials in CI.",
    systems: ["GitHub Actions / GitLab CI / Jenkins / Argo CD", "The security-scan integrations", "Cloud OIDC federation"],
    owners: ["DevOps / platform — owns pipelines", "AppSec — owns the gates", "Release management"],
    tools: ["who can modify pipeline YAML / Jenkinsfiles (branch protection on the pipeline config itself)", "confirm SAST/SCA/secret/image gates fail the build (not continue-on-error)", "deploy stage: required approvals + environment protection rules", "CI→cloud auth: OIDC short-lived vs stored long-lived keys"],
    finding: "Security scans run with continue-on-error (so they never block), any developer can edit the deploy workflow, and the pipeline holds a long-lived AWS admin key instead of OIDC — a compromised PR could deploy anything.",
    refs: [["NIST SP 800-204D", "https://csrc.nist.gov/pubs/sp/800/204/d/final"], ["OWASP Top 10 CI/CD Security Risks", "https://owasp.org/www-project-top-10-ci-cd-security-risks/"]],
  },
  "bcd-03": {
    artifacts: ["The image inventory in the registry + their vulnerability-scan results (base + app layers)", "Image-hardening evidence (minimal/distroless base, non-root user, no secrets baked in)", "The approved-base-image policy + provenance (only trusted bases)", "Image signing + admission policy (only signed images run)"],
    test: "Verify container images are minimal, scanned, hardened, and trusted. PASS: images use approved minimal/distroless bases, run as non-root, contain no embedded secrets, are vuln-scanned with no unaddressed criticals, and are signed (cosign) with admission control enforcing signatures. Exceptions: images with critical CVEs deployed, running as root, secrets baked into layers, untrusted/public base images, and unsigned images allowed to run.",
    systems: ["Registry (ECR/ACR/Harbor) + scanner (Trivy/Grype/Inspector)", "Sigstore cosign + admission control (Kyverno / OPA Gatekeeper)", "Base-image policy"],
    owners: ["Platform / DevOps — own images", "AppSec — owns the policy", "Security"],
    tools: ["registry/Trivy scan: images with critical CVEs; flag root user + embedded secrets (trivy + dockle)", "confirm minimal/distroless bases vs full-OS images", "cosign verify + confirm admission control blocks unsigned", "scan image layers for hardcoded secrets (trivy secret / docker history)"],
    finding: "Production images run as root off a full Ubuntu base with dozens of critical CVEs and an embedded API key in an early layer; nothing enforces signing, so any image can run.",
    refs: [["NIST SP 800-190 Container Security", "https://csrc.nist.gov/pubs/sp/800/190/final"], ["CIS Docker Benchmark", "https://www.cisecurity.org/benchmark/docker"]],
  },
  "bcd-04": {
    artifacts: ["The runtime security config (read-only root FS, dropped capabilities, no privileged containers, seccomp/AppArmor)", "The runtime-threat-detection tooling + its coverage (Falco / Defender for Containers)", "The privileged / host-mounted container inventory", "The runtime network policy (pod-to-pod restriction)"],
    test: "Verify containers run with least privilege and runtime threats are detected. PASS: containers are non-privileged, drop Linux capabilities, use read-only root filesystems + seccomp/AppArmor, and don't mount the host or run as root; runtime threat detection (Falco) is deployed; and network policy restricts pod-to-pod. Exceptions: privileged containers, host-path/Docker-socket mounts, no seccomp, no runtime detection, and flat pod networking.",
    systems: ["Kubernetes runtime (containerd) + Pod Security Standards / admission", "Runtime detection — Falco / Defender for Containers / Sysdig", "CNI network policy (Calico / Cilium)"],
    owners: ["Platform / SRE (Kubernetes)", "Security (runtime detection)", "App owners"],
    tools: ["scan running pods for privileged:true, hostPID/hostNetwork, host-path/docker.sock mounts, runAsRoot", "confirm Pod Security Standards (restricted) admission is enforced", "Falco / Defender-for-Containers deployment + rule coverage", "NetworkPolicy presence (default-deny pod-to-pod?)"],
    finding: "Several workloads run privileged with the Docker socket mounted (container escape to the node), no seccomp profiles are applied, there's no runtime detection, and any pod can reach any other pod.",
    refs: [["NIST SP 800-190", "https://csrc.nist.gov/pubs/sp/800/190/final"], ["CIS Kubernetes Benchmark", "https://www.cisecurity.org/benchmark/kubernetes"]],
  },
  "bcd-05": {
    artifacts: ["The Kubernetes cluster config (API-server access, RBAC, etcd encryption, audit logging) vs the CIS benchmark", "The cluster RBAC export — who/what has cluster-admin", "Control-plane + node hardening + version/patch (supported, not EOL)", "Admission control + Pod Security + network-policy enforcement"],
    test: "Verify orchestration clusters are hardened and access-controlled. PASS: the API server isn't publicly exposed without auth; RBAC is least-privilege (few cluster-admins, no wildcard roles, no anonymous access); etcd is encrypted at rest; audit logging is on and shipped; admission control (Pod Security, OPA) is enforced; and the cluster + nodes are patched/supported. Exceptions: public API server, broad cluster-admin/wildcard RBAC, anonymous access, etcd unencrypted, audit logging off, and EOL cluster versions.",
    systems: ["Kubernetes (EKS / AKS / GKE / self-managed)", "kube-bench / CIS-CAT", "RBAC + admission control"],
    owners: ["Platform / SRE — owns the cluster", "Security — owns the baseline", "Cloud platform"],
    tools: ["kube-bench (CIS Kubernetes Benchmark) on control plane + nodes", "kubectl get clusterrolebindings → who has cluster-admin; find wildcard ClusterRoles + system:anonymous binding", "confirm etcd encryption-at-rest + API audit logging enabled + shipped", "check cluster version vs vendor EOL"],
    finding: "The cluster grants cluster-admin via a wildcard role to a CI service account, the API audit log is disabled, etcd isn't encrypted, and the cluster is two versions past end-of-life.",
    refs: [["CIS Kubernetes Benchmark", "https://www.cisecurity.org/benchmark/kubernetes"], ["NSA/CISA Kubernetes Hardening Guide", "https://www.cisa.gov/news-events/alerts/2022/03/15/updated-kubernetes-hardening-guide"]],
  },
  "bcd-06": {
    artifacts: ["Per-workload security context + resource limits + the service-account/identity it runs as", "The workload-to-cloud-identity mapping (IRSA / workload identity vs node role)", "Workload network exposure (Services/Ingress exposed externally) + their auth", "Secrets mounted into workloads + how (env vs mounted secret vs vault injection)"],
    test: "Verify individual workloads run least-privilege with scoped identity. PASS: each workload uses a dedicated, least-privilege service account mapped to a scoped cloud identity (IRSA / workload identity, not the node role); has resource limits; restricts its network exposure with auth on anything external; and consumes secrets via a vault/CSI driver, not baked env vars. Exceptions: workloads using the node role or default SA (over-privileged), no resource limits, externally-exposed services with no auth, and secrets in plain env vars.",
    systems: ["Kubernetes workloads + ServiceAccounts", "IRSA / GKE / Azure Workload Identity", "Secrets CSI / Vault injector"],
    owners: ["App owners — own their workloads", "Platform / SRE", "Security"],
    tools: ["per workload: serviceAccountName + its RBAC + the cloud identity it assumes (IRSA annotation)", "find pods using the default SA or the node instance role (over-privileged)", "list externally-exposed Services/Ingress + whether they require auth", "how secrets reach pods: env vs mounted vs vault-injected"],
    finding: "Most pods use the default service account and inherit the node's broad IAM role (so a compromised pod has the node's cloud permissions), and database passwords are passed as plain environment variables.",
    refs: [["NIST SP 800-190", "https://csrc.nist.gov/pubs/sp/800/190/final"], ["CIS Kubernetes Benchmark", "https://www.cisecurity.org/benchmark/kubernetes"]],
  },
  "bcd-07": {
    artifacts: ["The provenance + admission policy for what's allowed to run (signed images, trusted registries, SLSA level)", "Helm-chart/manifest source trust (charts from trusted repos, pinned versions)", "The admission-control policy set (Kyverno / OPA Gatekeeper) enforcing supply-chain rules", "Third-party operators/controllers running in-cluster + their permissions"],
    test: "Verify only trusted, provenance-verified software runs in the cluster. PASS: admission control enforces signed images from approved registries with provenance/SLSA attestation; Helm charts/manifests come from trusted, pinned sources; third-party operators are vetted and least-privilege; and there's no path to deploy unverified artifacts. Exceptions: any-registry pulls, unsigned images admitted, unpinned/untrusted Helm charts, and cluster-wide operators with excessive permissions.",
    systems: ["Admission control (Kyverno / OPA Gatekeeper / Sigstore policy-controller)", "Registry allow-list", "Helm / GitOps source"],
    owners: ["Platform / SRE — owns admission", "AppSec — owns supply-chain policy", "Security"],
    tools: ["Kyverno/Gatekeeper policy: enforce signed + approved-registry + provenance", "test: try to deploy an unsigned/public image — is it blocked?", "review third-party operators (their ClusterRole permissions)", "Helm chart sources + version pinning"],
    finding: "Admission control is in audit mode only, so unsigned images from public registries deploy freely, and a third-party logging operator holds cluster-admin-equivalent permissions.",
    refs: [["SLSA", "https://slsa.dev/"], ["NSA/CISA Kubernetes Hardening Guide", "https://www.cisa.gov/news-events/alerts/2022/03/15/updated-kubernetes-hardening-guide"]],
  },
  "bcd-08": {
    artifacts: ["Branch-protection + required-review (CODEOWNERS) config on the default/release branches", "Signed-commit + required-status-check config before merge", "The repo access model (who can push/merge/admin; outside collaborators)", "The required CI checks and whether they can be bypassed/admin-overridden"],
    test: "Verify source-code integrity controls prevent unreviewed/malicious code reaching builds. PASS: protected branches require PR review (CODEOWNERS for sensitive paths), passing status checks, and (ideally) signed commits; direct push + force-push to protected branches are blocked; admin bypass is restricted + logged; and repo access is least-privilege. Exceptions: unprotected default branch, no required review, admins routinely bypassing checks, force-push allowed, and broad write/admin access.",
    systems: ["GitHub/GitLab/Bitbucket branch protection + CODEOWNERS", "Signed commits (GPG / Sigstore gitsign)", "SCM access model"],
    owners: ["Engineering org / repo admins", "AppSec", "DevOps"],
    tools: ["export branch-protection rules per repo: required reviews, CODEOWNERS, status checks, signed commits, no force-push", "list who has admin/maintain/write; outside collaborators", "check 'allow admins to bypass' + recent bypass events", "confirm required CI checks can't be skipped"],
    finding: "The release branch allows direct pushes with no required review, signed commits aren't required, and three outside collaborators have write access — unreviewed code can reach the build unchecked.",
    refs: [["NIST SSDF (SP 800-218) — PS practices", "https://csrc.nist.gov/pubs/sp/800/218/final"], ["SLSA", "https://slsa.dev/"]],
  },
  "bcd-09": {
    artifacts: ["The inventory of secrets in CI/CD (repo/org/environment secrets, stored credentials) + their scope", "Evidence secrets aren't exposed to untrusted workflows (fork PRs) or printed in logs", "Use of short-lived/OIDC credentials vs long-lived stored secrets", "Secret rotation + access scoping (which workflows can read which secrets)"],
    test: "Verify CI/CD secrets are minimal, scoped, short-lived, and not exposed. PASS: cloud auth uses OIDC short-lived federation (not stored long-lived keys); secrets are scoped to specific environments/workflows; fork/PR workflows can't access secrets; secrets are masked in logs and rotated; and there's no plaintext credential in pipeline config. Exceptions: long-lived cloud keys stored as CI secrets, org-wide secrets readable by every workflow, secrets exposed to fork-PR runs, secrets echoed in logs, and unrotated CI credentials.",
    systems: ["CI secret stores (GitHub/GitLab secrets) + cloud OIDC", "Vault / Secrets Manager", "The pipeline config"],
    owners: ["DevOps / platform security", "AppSec", "Cloud platform"],
    tools: ["inventory repo/org/environment secrets + which workflows can read them", "confirm cloud auth is OIDC (id-token) not a stored long-lived key", "check pull_request_target / fork-PR workflows can't reach secrets", "scan pipeline logs for leaked/echoed secrets; confirm masking + rotation"],
    finding: "A long-lived AWS admin key is an org-wide secret readable by every workflow including fork PRs, and a recent build log printed it in plaintext — full cloud compromise from one contributor.",
    refs: [["OWASP CI/CD Security Risks", "https://owasp.org/www-project-top-10-ci-cd-security-risks/"], ["GitHub OIDC Hardening", "https://docs.github.com/actions/deployment/security-hardening-your-deployments"]],
  },
  "bcd-10": {
    artifacts: ["The audit-log coverage for SCM, CI/CD, and registry (who did what: merges, pipeline edits, deploys, secret access, image pushes)", "Forwarding of those logs to the SIEM + retention", "Detections on high-risk build events (pipeline modified, secret accessed, admin bypass, image push to prod)", "Tamper-resistance of the build audit trail"],
    test: "Verify build/CI/CD/registry activity is logged, retained, and monitored. PASS: SCM + CI/CD + registry emit audit logs (merges, workflow changes, deploys, secret access, image pushes, admin actions); logs ship to the SIEM with retention; detections fire on high-risk events (pipeline tampering, secret access by a new actor, prod deploy outside the change window); and the trail is tamper-resistant. Exceptions: build systems not logging to the SIEM, no detections on pipeline/secret abuse, short retention, and logs editable by the same admins.",
    systems: ["GitHub/GitLab audit log, CI/CD logs, registry logs", "SIEM", "Detection content"],
    owners: ["Security operations / detection engineering", "DevOps (emits logs)", "SOC"],
    tools: ["confirm SCM/CI/registry audit logs forward to the SIEM (e.g. GitHub audit-log streaming)", "retention check", "detections for: workflow file changed, secret accessed, admin bypass, image push to a prod tag, new deploy credential", "verify the build audit trail can't be edited by the dev admins"],
    finding: "GitHub audit-log streaming isn't enabled, so SCM events never reach the SIEM, and there are no detections for pipeline tampering or secret access — a malicious workflow edit would be invisible.",
    refs: [["NIST SP 800-92", "https://csrc.nist.gov/pubs/sp/800/92/final"], ["OWASP CI/CD Security Risks", "https://owasp.org/www-project-top-10-ci-cd-security-risks/"]],
  },
  "bcd-11": {
    artifacts: ["The SoD matrix for the SDLC (who can code, who approves, who deploys to prod) + the actual role assignments", "Evidence that the same person can't author + approve + deploy their own change to production unchecked", "Production-deploy authorisation (separate from code authorship)", "The break-glass deploy process + its logging"],
    test: "Verify duties are separated across the build-to-deploy flow. PASS: code authorship, review/approval, and production deployment are separated so no single person can push their own code to prod unreviewed; prod-deploy requires a distinct approver; emergency/break-glass deploys are logged + reviewed. Exceptions: a developer who can author, self-approve, and deploy to prod; shared deploy accounts that mask who deployed; and break-glass with no logging/review.",
    systems: ["SCM (review/merge rights) + CI/CD (deploy rights)", "Environment protection rules", "IGA (role assignments)"],
    owners: ["Engineering leadership + AppSec", "Release management", "Audit / GRC"],
    tools: ["map who can: merge to the release branch vs approve vs trigger prod deploy (separated per person?)", "find individuals holding all three (self-service to prod)", "environment protection: required reviewers distinct from the PR author", "break-glass deploy logging + review"],
    finding: "Five engineers can author code, approve their own PR (admin bypass), and trigger production deploys — no separation of duties, so one person can ship unreviewed code to prod.",
    refs: [["NIST SP 800-53 AC-5 Separation of Duties", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"], ["SLSA (two-person review)", "https://slsa.dev/"]],
  },
  "bcd-12": {
    artifacts: ["The inventory of AI coding tools + agentic CI workloads in use (Copilot, AI review, autonomous build/fix agents) + their permissions", "Guardrails on AI-generated code (human review required, no auto-merge, scanning of AI output)", "Data-handling for AI dev tools (proprietary code sent to which model endpoints; training opt-out)", "The permission scope + logging of any agent that can commit/build/deploy"],
    test: "Govern AI-assisted development and agentic workloads. PASS: AI coding tools are approved + inventoried; AI-generated code goes through the same review + security gates as human code (no auto-merge to protected branches); agentic build/fix workloads run least-privilege with a human-in-the-loop for merges/deploys and full logging; and proprietary code isn't sent to ungoverned model endpoints (training opt-out, approved tenancy). Exceptions: shadow AI dev tools, AI/agent code auto-merged without review, agents with broad commit/deploy authority, and source code leaking to public models.",
    systems: ["GitHub Copilot / Cursor / AI review tools", "Agentic CI workloads + their service-account permissions", "DLP / approved-model tenancy"],
    owners: ["Engineering + AppSec", "AI governance", "Platform security"],
    tools: ["inventory AI dev tools + agentic CI agents and their repo/deploy permissions", "confirm AI-generated PRs require human review + pass security gates (no auto-merge)", "check agent service accounts are least-privilege + their actions logged/attributable", "confirm source isn't sent to public model endpoints (enterprise tenancy + training opt-out)"],
    finding: "An autonomous 'auto-fix' agent has write access to all repos and can merge its own PRs to protected branches with no human review, and developers use a personal AI tool that sends proprietary code to a public endpoint with training enabled.",
    refs: [["NIST AI Risk Management Framework", "https://www.nist.gov/itl/ai-risk-management-framework"], ["OWASP Top 10 for LLM Applications", "https://owasp.org/www-project-top-10-for-large-language-model-applications/"]],
  },
  "bcd-13": {
    artifacts: ["The SBOM (Software Bill of Materials) generated for each build/image (CycloneDX/SPDX) + where stored", "Provenance/attestation (SLSA) linking artifact → source commit → build", "SBOM coverage (builds/images with an SBOM vs total)", "The process to query SBOMs for impact (e.g. 'which images contain log4j x.y')"],
    test: "Verify every artifact has an SBOM and verifiable provenance. PASS: each build produces an SBOM (CycloneDX/SPDX) stored + associated with the image; SLSA provenance attests the artifact was built from the reviewed source on the trusted builder; SBOM coverage is near-complete; and SBOMs are queryable for vulnerability impact. Exceptions: images with no SBOM, no build provenance (can't prove what's inside or where it came from), and no way to answer 'which artifacts contain this vulnerable component'.",
    systems: ["SBOM tooling (Syft, cdxgen) + storage (registry referrers / Dependency-Track)", "SLSA provenance (slsa-github-generator, cosign attest)", "The build pipeline"],
    owners: ["AppSec / platform — own SBOM/provenance", "DevOps", "Security"],
    tools: ["confirm each build generates + stores an SBOM (Syft → CycloneDX) attached to the image", "verify SLSA provenance attestation (cosign verify-attestation)", "SBOM coverage: images with an SBOM vs total in the registry", "test an impact query: 'which images contain component X' via Dependency-Track"],
    finding: "Only a handful of images have SBOMs and none have build provenance, so when the next Log4Shell hits, there's no way to know which of the 400 production images are affected.",
    refs: [["CISA / NTIA SBOM", "https://www.cisa.gov/sbom"], ["SLSA", "https://slsa.dev/"]],
  },
  "bcd-14": {
    artifacts: ["The build-time security gates: SAST, SCA (dependencies), secret scan, image scan, IaC scan, DAST — and which are wired into the pipeline", "Coverage (repos/pipelines with each gate) + whether gates block or warn", "Findings + remediation SLA + the suppressed/accepted-findings register", "DAST / dynamic testing of the running app pre-prod"],
    test: "Verify security testing is built into the pipeline and gates risky releases. PASS: SAST + SCA + secret-scan + image-scan + IaC-scan run on every pipeline and BLOCK on criticals; DAST runs against pre-prod; coverage spans repos/pipelines; findings have remediation SLAs; and suppressions are justified + reviewed. Exceptions: missing scan types (e.g. no SCA), gates in warn-only mode, large coverage gaps, no DAST, and blanket suppressions hiding real findings.",
    systems: ["SAST (CodeQL/Semgrep/SonarQube), SCA (Snyk/Dependabot/Trivy), secret scan, image scan, IaC scan (Checkov), DAST (ZAP/Burp)", "The CI pipeline", "Findings / ASPM platform"],
    owners: ["AppSec — owns the testing program", "Developers — remediate", "DevOps (wiring)"],
    tools: ["per pipeline: which gates exist (SAST/SCA/secret/image/IaC/DAST) + block vs warn", "coverage = repos with the gate ÷ total repos", "findings + remediation SLA + the suppression register (justified?)", "confirm DAST runs against a deployed pre-prod build"],
    finding: "Half the repos have no SCA (so vulnerable dependencies ship freely), SAST runs warn-only, there's no DAST, and a blanket suppression hides 200 'won't fix' findings including criticals.",
    refs: [["NIST SSDF (SP 800-218)", "https://csrc.nist.gov/pubs/sp/800/218/final"], ["OWASP DevSecOps Guideline", "https://owasp.org/www-project-devsecops-guideline/"]],
  },
  "bcd-15": {
    artifacts: ["The open-source license inventory per application (from SCA/SBOM) + each component's license", "The org's license policy (allowed / restricted / prohibited — e.g. GPL/AGPL copyleft handling)", "License-policy violations (prohibited or incompatible licenses in shipped products)", "Attribution/notice compliance (NOTICE files, source-offer obligations)"],
    test: "Verify open-source license obligations are identified and met. PASS: SCA/SBOM enumerates every component's license; a license policy classifies allowed/restricted/prohibited; the pipeline flags policy violations (e.g. AGPL in a distributed product) before release; and attribution/notice + copyleft obligations are met. Exceptions: unknown-license components shipped, prohibited/incompatible licenses (copyleft in proprietary distributed software), missing attribution/NOTICE, and no gate catching license risk.",
    systems: ["SCA / license tooling (Snyk / FOSSA / Black Duck / Trivy license)", "SBOM", "Legal license policy"],
    owners: ["AppSec / Open-Source Program Office", "Legal", "Developers"],
    tools: ["SCA license report per app (FOSSA / Black Duck / Trivy --scanners license)", "map components to the license policy (allowed / restricted / prohibited)", "flag copyleft (GPL/AGPL) in distributed/proprietary products", "confirm NOTICE/attribution files + source-offer obligations are met"],
    finding: "A distributed product bundles an AGPL component (triggering copyleft source-disclosure obligations the company isn't meeting), and dozens of dependencies have unknown or unvetted licenses with no NOTICE file.",
    refs: [["SPDX License List", "https://spdx.org/licenses/"], ["OpenChain ISO/IEC 5230", "https://www.openchainproject.org/"]],
  },

  // ── Infrastructure as Code (IaC) ────────────────────────────────────────────
  "iac-01": {
    artifacts: [
      "The pull-request history for the IaC repos — every infrastructure change as a PR with its reviewers and approval timestamps",
      "The branch-protection + CODEOWNERS export for the IaC repos (who must approve a change to prod modules/environments)",
      "The merge-vs-deploy log: which merged PR produced which `terraform apply` / stack update, and who triggered it",
      "The exception list of changes applied out-of-band (console clicks / `terraform apply` from a laptop, bypassing the PR flow)",
    ],
    test: "Trace every infrastructure change in the period from running state back to a reviewed, approved pull request. PASS: each `terraform apply` / CloudFormation stack update maps to a merged PR that had at least one independent CODEOWNER approval before merge; the plan in the PR matches what was applied; direct console/CLI changes are blocked or exception-tracked; and the author can't self-approve their own prod change. Exceptions: applies with no corresponding PR ('clickops' or laptop applies), PRs self-approved or merged with no review, and a divergence between the reviewed plan and what actually ran.",
    systems: ["GitHub / GitLab / Bitbucket (the IaC repos + branch protection + CODEOWNERS)", "Terraform Cloud / HCP Terraform or Atlantis (run + apply log)", "AWS CloudTrail / Azure Activity Log (to catch out-of-band changes)"],
    owners: ["Platform / Cloud engineering — authors and merges IaC", "SRE / module owners — the CODEOWNER approvers", "Security engineering — reviews the SoD on prod environments"],
    tools: [
      "GitHub: gh api repos/{org}/{repo}/branches/main/protection  (required reviews, CODEOWNERS, no self-review)",
      "HCP Terraform: GET /api/v2/workspaces/{id}/runs  → each apply, its source PR (commit SHA) and the user who confirmed it",
      "Atlantis: the PR-comment apply trail (plan → approve → apply) per repo",
      "AWS:  CloudTrail lookup for ConsoleLogin + non-Terraform-principal mutating events on managed resources (out-of-band changes)",
    ],
    finding: "A cluster of production security-group and IAM changes applied directly from an engineer's laptop with no PR, plus two PRs the author self-approved and merged — infrastructure changed prod with no independent review.",
    refs: [["NIST SSDF SP 800-218 (PO/PS — reviewed change)", "https://csrc.nist.gov/pubs/sp/800/218/final"], ["HashiCorp — Terraform run/apply controls", "https://developer.hashicorp.com/terraform/cloud-docs/run/remote-operations"], ["NIST SP 800-53 CM-3 Configuration Change Control", "https://csrc.nist.gov/projects/risk-management/sp800-53-controls"]],
  },
  "iac-02": {
    artifacts: [
      "The IaC-scanner configuration in the pipeline (Checkov / tfsec / Terrascan / KICS) — which repos run it and the ruleset/severity threshold",
      "A current scan report (SARIF/JSON) across the IaC repos with each misconfiguration, its policy id, and severity",
      "Evidence the scan is a blocking gate, not advisory (the CI step's failure behaviour on a HIGH/CRITICAL finding)",
      "The suppression / baseline file (`.checkov.yaml` skips, `#tfsec:ignore`, `#checkov:skip`) with justifications",
    ],
    test: "Run the IaC security scanners across the in-scope IaC repos and compare to the pipeline gate. PASS: a misconfiguration scanner (Checkov/tfsec/Terrascan/KICS) runs on every IaC repo on each PR, fails the build on findings at/above the agreed severity, covers Terraform + CloudFormation + Kubernetes/Helm manifests, and every suppression has a documented, time-boxed justification. Exceptions: IaC repos with no scanner, the scan set to soft-fail/warn-only, scanners run only locally (not enforced in CI), and blanket `skip-check`/ignore rules hiding real HIGH/CRITICAL findings.",
    systems: ["Checkov / tfsec / Terrascan / KICS (the IaC SAST scanners)", "GitHub Actions / GitLab CI / Jenkins (the gate)", "DefectDojo / ASPM platform (findings aggregation)"],
    owners: ["Security engineering / AppSec — owns the rulesets and gate", "Platform engineering — wires the scan into CI", "Module owners — remediate findings"],
    tools: [
      "checkov -d . --compact --output sarif   (and confirm CI uses `--soft-fail` is NOT set)",
      "tfsec . --minimum-severity HIGH --format sarif",
      "terrascan scan -i terraform -t aws   /   kics scan -p . --report-formats sarif",
      "grep the repos for suppression markers: `#checkov:skip`, `#tfsec:ignore`, `.checkov.yaml` skip lists — and check each has a reason",
    ],
    finding: "Checkov runs with `soft_fail: true` so it never blocks a merge; a current scan surfaces ~40 HIGH findings (unencrypted EBS, public S3, 0.0.0.0/0 ingress on 22/3389) that have been shipping for months, and a repo-wide skip silently disables the encryption rules.",
    refs: [["OWASP DevSecOps Guideline — IaC scanning", "https://owasp.org/www-project-devsecops-guideline/"], ["Checkov documentation", "https://www.checkov.io/"], ["NIST SP 800-204D — pipeline security testing", "https://csrc.nist.gov/pubs/sp/800/204/d/final"]],
  },
  "iac-03": {
    artifacts: [
      "The `terraform plan` JSON (`terraform show -json plan.out`) for in-scope changes — the resources and attributes about to be created",
      "A CSPM/posture export (Wiz / Prisma Cloud / AWS Security Hub / Defender for Cloud) of the resources these IaC repos manage",
      "The mapping of high-risk resource types to their secure-config requirements (public access, encryption, logging, network exposure)",
      "The set of resources flagged public/unencrypted/over-exposed in runtime that trace back to an IaC definition",
    ],
    test: "Inspect the IaC for the specific cloud misconfigurations that create exposure, and confirm runtime posture agrees. PASS: storage is private + encrypted (no public S3/blob ACLs, KMS/CMK encryption on at rest), security groups/NSGs don't allow 0.0.0.0/0 to admin ports (22/3389/databases), IAM in code is least-privilege (no `\"Action\": \"*\"` / `\"Resource\": \"*\"`), and logging (flow logs, CloudTrail, bucket access logs) is enabled in the template — and the CSPM shows zero of these in runtime. Exceptions: any IaC resource defaulting a bucket public, an `0.0.0.0/0` ingress on a sensitive port, a wildcard IAM policy, or encryption/logging disabled — each named by resource address and file.",
    systems: ["Terraform / CloudFormation / Bicep / Pulumi (the templates)", "AWS Security Hub / Wiz / Prisma Cloud / Microsoft Defender for Cloud (runtime CSPM)", "Cloud provider config (S3 Block Public Access, EBS default encryption)"],
    owners: ["Cloud / Platform engineering — owns the resource definitions", "Cloud security — owns the CSPM and the secure-config baseline", "FinOps / app owners — own specific resources"],
    tools: [
      "terraform plan -out plan.out && terraform show -json plan.out  → jq the planned_values for public ACLs, 0.0.0.0/0, wildcard IAM",
      "checkov / tfsec policy ids CKV_AWS_18/20/23/24 (logging, public bucket, open SG) against the plan",
      "AWS: aws securityhub get-findings --filters '{\"GeneratorId\":[{\"Value\":\"aws-foundational-security-best-practices\"}]}'",
      "Wiz/Prisma query: 'publicly exposed resources' and 'unencrypted data stores' joined back to the owning Terraform module",
    ],
    finding: "A reusable storage module defaults the bucket policy to allow public read and omits the encryption block, so every one of the ~30 buckets instantiated from it is public and unencrypted — the single-template-many-resources multiplication of one mistake (the Capital One / public-bucket class).",
    refs: [["CIS AWS Foundations Benchmark", "https://www.cisecurity.org/benchmark/amazon_web_services"], ["AWS Foundational Security Best Practices", "https://docs.aws.amazon.com/securityhub/latest/userguide/fsbp-standard.html"], ["OWASP Cloud-Native Security", "https://owasp.org/www-project-devsecops-guideline/"]],
  },
  "iac-04": {
    artifacts: [
      "The policy-as-code bundle (OPA/Rego, Sentinel, or Conftest policies) and which workspaces/stacks it's bound to",
      "The policy-set enforcement levels in HCP Terraform / cloud (advisory vs soft-mandatory vs hard-mandatory)",
      "A recent run where a non-compliant plan was actually blocked by policy (the proof it enforces, not just reports)",
      "The mapping of compliance requirements (CIS, PCI, internal standards) to the codified policy rules — and the gaps",
    ],
    test: "Verify compliance is enforced as code in the plan/apply path, not just checked after the fact. PASS: a policy engine (OPA/Conftest, Sentinel, or CloudFormation Guard) evaluates every plan before apply; controls that must never be violated (no public data store, encryption required, mandatory tags, approved regions) are HARD-mandatory (block); enforcement levels are deliberate and documented; and the policy set demonstrably blocked a real non-compliant change. Exceptions: policy set in advisory mode only (logs but never blocks), critical controls missing from the codified policy, override/bypass available to the requester, and compliance frameworks with no corresponding policy.",
    systems: ["Open Policy Agent / Conftest (Rego)", "HashiCorp Sentinel / HCP Terraform policy sets", "AWS CloudFormation Guard / AWS Config conformance packs", "Pulumi CrossGuard"],
    owners: ["Security engineering / Governance — authors policy-as-code", "Platform engineering — binds policies to workspaces", "Compliance / GRC — maps requirements to policy"],
    tools: [
      "conftest test plan.json -p policy/   (Rego policies against the terraform plan JSON)",
      "HCP Terraform: GET /api/v2/policy-sets and /runs/{id}/policy-checks  → enforcement level + pass/fail per run",
      "cfn-guard validate -r rules.guard -d template.json",
      "verify a deliberately non-compliant plan (public bucket) is HARD-blocked, not just flagged advisory",
    ],
    finding: "Every Sentinel/OPA policy is set to advisory enforcement, so a plan creating a public, unencrypted bucket logs a warning and applies anyway; key controls (approved-regions, no-wildcard-IAM) aren't codified at all, so 'policy as code' provides reporting but no actual guardrail.",
    refs: [["HashiCorp — Policy Enforcement (Sentinel/OPA)", "https://developer.hashicorp.com/terraform/cloud-docs/policy-enforcement"], ["Open Policy Agent", "https://www.openpolicyagent.org/docs/latest/"], ["CIS Benchmarks", "https://www.cisecurity.org/cis-benchmarks"]],
  },
  "iac-05": {
    artifacts: [
      "The private module registry inventory (HCP Terraform private registry / Terraform Registry / Git module sources) and their version constraints",
      "The module source/version pinning in callers (`source` + `version` / git ref) — pinned to an immutable version vs `latest`/floating branch",
      "The module-publishing workflow: who can publish a new module version, and whether published modules are scanned/reviewed/signed",
      "Provenance for third-party / public modules consumed (publisher trust, fork-and-vet evidence)",
    ],
    test: "Verify reusable modules and templates are governed, versioned, and trusted. PASS: callers consume modules from an approved private registry pinned to immutable, semver-tagged versions (not a moving branch or `latest`); module publishing is restricted to module owners and the module passes scanning/review before a version is cut; third-party/public modules are vetted (forked into the registry or pinned to a reviewed commit); and a known-good module change propagates through controlled version bumps, not silently. Exceptions: modules sourced from a floating `main`/`master` branch (any push changes prod), unrestricted publishing, public modules pulled directly from the internet with no review, and unpinned/`latest` references.",
    systems: ["HCP Terraform private module registry / Terraform Registry", "Git module sources (GitHub/GitLab) + tags", "Module CI (scan + test before publish)", "Pulumi/Bicep module/registry equivalents"],
    owners: ["Platform engineering — owns the module library / golden modules", "Module owners — publish and version", "Security engineering — reviews module security"],
    tools: [
      "grep callers for `source =` + `version =` / git `?ref=` → flag any pointing at a branch (`?ref=main`) or with no version constraint",
      "HCP Terraform: GET /api/v2/organizations/{org}/registry-modules  → versions, publishers",
      "confirm module repos run Checkov/tfsec + tests on PR before a tag is published",
      "for public modules: confirm they're vendored into the private registry or pinned to a reviewed commit SHA, not `terraform-aws-modules/...` floating",
    ],
    finding: "The shared 'vpc' and 'eks' modules are referenced as `?ref=main`, so any merge to those module repos immediately changes every consuming environment with no version review; module publishing is open to all engineers and modules aren't scanned before a tag is cut.",
    refs: [["HashiCorp — Module versioning & private registry", "https://developer.hashicorp.com/terraform/language/modules/syntax"], ["NIST SSDF SP 800-218 (PW.4 — reuse secure components)", "https://csrc.nist.gov/pubs/sp/800/218/final"], ["CIS Software Supply Chain Security Guide", "https://www.cisecurity.org/insights/white-papers/cis-software-supply-chain-security-guide"]],
  },
  "iac-06": {
    artifacts: [
      "A current `terraform plan` against the live state for in-scope workspaces (a non-empty plan = drift between code and runtime)",
      "The drift-detection job output (Terraform Cloud health assessments / driftctl / `terraform plan -detailed-exitcode` on a schedule)",
      "AWS Config / Azure Policy compliance timelines showing out-of-band resource changes",
      "The reconciliation record: for each drift, whether it was re-applied to match code or code updated to match reality, and the change ticket",
    ],
    test: "Detect divergence between the declared IaC state and running infrastructure. PASS: a scheduled drift check (HCP Terraform health assessments, `terraform plan -detailed-exitcode`, or driftctl) runs across managed workspaces; a clean plan (exit 0 / no changes) is the steady state; any drift is alerted, investigated, and reconciled (re-applied or codified) within SLA; and out-of-band console changes are correlated to a change record. Exceptions: workspaces with no scheduled drift check, persistent unreconciled drift (runtime silently diverged from code), and unauthorized changes (drift with no corresponding change ticket) — e.g. a security group widened in the console that the code would revert.",
    systems: ["Terraform / HCP Terraform (state + health assessments / continuous validation)", "driftctl / CloudQuery (drift between code and cloud)", "AWS Config / Azure Policy (resource-change history)"],
    owners: ["Platform / SRE — owns state and drift remediation", "Cloud security — investigates security-relevant drift", "Change management — correlates authorized vs not"],
    tools: [
      "terraform plan -detailed-exitcode   (exit 2 = drift) per workspace on a schedule",
      "HCP Terraform: enable + read 'health assessments' (drift detection) via GET /api/v2/workspaces/{id}/assessment-results",
      "driftctl scan --from tfstate+s3://... --to aws+tf   (IaC state vs actual cloud)",
      "AWS: aws configservice get-resource-config-history  → who changed a managed resource out-of-band, when",
    ],
    finding: "Drift detection isn't scheduled on any workspace; an ad-hoc `terraform plan` reveals dozens of resources changed in the console (a security group manually opened to 0.0.0.0/0, an RDS instance resized, IAM policies edited) that the code doesn't reflect and would silently revert on the next apply.",
    refs: [["HashiCorp — Drift detection / health assessments", "https://developer.hashicorp.com/terraform/cloud-docs/workspaces/health"], ["NIST SP 800-128 — Configuration Management", "https://csrc.nist.gov/pubs/sp/800/128/final"], ["CIS Control 4 — Secure Configuration", "https://www.cisecurity.org/controls"]],
  },
  "iac-07": {
    artifacts: [
      "A secret-scan of the IaC repos + their history (gitleaks / TruffleHog) for credentials, keys, and tokens committed in `.tf`/`.yaml`/`tfvars`",
      "The Terraform/Pulumi state-file storage config — where state lives, whether it's encrypted, and who can read it (state contains secrets in plaintext)",
      "Evidence that secrets are referenced from a vault at apply time (Vault provider / cloud secret-manager data sources) rather than hardcoded or in `terraform.tfvars`",
      "The IaC execution credential model — OIDC/short-lived role assumption for the runner vs a long-lived stored cloud key",
    ],
    test: "Verify no secrets live in IaC code, history, or state, and that apply-time auth is short-lived. PASS: no hardcoded credentials/keys/tokens in the IaC repos or git history; secrets are injected at apply time from a secrets manager (Vault, AWS/GCP/Azure secret-manager data sources) or passed as protected CI variables — never committed `tfvars`; remote state is encrypted at rest with tightly scoped read access (state stores resource secrets like DB passwords in plaintext); and the IaC runner authenticates to the cloud via OIDC/short-lived role, not a long-lived key. Exceptions: any secret found in code or history, state stored unencrypted or world-readable, `terraform.tfvars` with passwords committed, and a long-lived admin cloud key embedded in the pipeline.",
    systems: ["gitleaks / TruffleHog (repo + history scan)", "Terraform/Pulumi remote state (S3+KMS / HCP Terraform / azurerm backend)", "HashiCorp Vault / AWS Secrets Manager / Azure Key Vault (apply-time injection)", "CI OIDC federation"],
    owners: ["Platform engineering — owns IaC and state backends", "Crypto / Secrets team — owns the vault integration", "Security engineering — owns secret-scanning"],
    tools: [
      "gitleaks detect --source . --log-opts='--all'   (full history)  /  trufflehog git file://. ",
      "confirm backend encryption: S3 backend has `encrypt = true` + a KMS key, and read access to the state bucket/HCP workspace is least-privilege",
      "grep for hardcoded secrets in `*.tf` / `*.tfvars` and confirm secret values come from `data.vault_*` / `data.aws_secretsmanager_secret_version`",
      "CI: confirm cloud auth is OIDC (`aws-actions/configure-aws-credentials` with role-to-assume) not a stored AWS_SECRET_ACCESS_KEY",
    ],
    finding: "A database password and an API token are hardcoded in `terraform.tfvars` committed to history, the S3 state backend has `encrypt` unset and is readable by the whole org (so every secret in state is exposed), and the pipeline authenticates with a long-lived AWS admin access key stored as a CI secret.",
    refs: [["OWASP Secrets Management Cheat Sheet", "https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html"], ["HashiCorp — Sensitive data in state", "https://developer.hashicorp.com/terraform/language/state/sensitive-data"], ["GitHub — OIDC hardening for cloud deploys", "https://docs.github.com/actions/deployment/security-hardening-your-deployments/about-security-hardening-with-openid-connect"]],
  },
  "iac-08": {
    artifacts: [
      "The IaC pipeline definition (plan → policy → approval → apply) and the protected-environment / required-approval config for prod applies",
      "The runner identity + permissions used by the apply stage (the cloud role the pipeline assumes) and its scope",
      "Evidence the plan that was reviewed is the exact plan applied (saved plan artifact `plan.out` reused at apply, not a re-plan)",
      "Who can trigger an apply, edit the pipeline, and approve a prod run (the SoD across the pipeline stages)",
    ],
    test: "Verify IaC change is delivered only through a controlled pipeline. PASS: applies run only from the pipeline (no local apply to prod), in the order plan → IaC-scan → policy-as-code → human approval → apply; the apply consumes the saved, reviewed plan artifact (so what's approved is what runs); prod applies require an approver distinct from the author via environment protection; the runner assumes a least-privilege, short-lived role scoped to the workspace; and the pipeline definition itself is branch-protected. Exceptions: developers applying to prod from laptops, the apply re-planning instead of using the approved plan (review/apply divergence), no approval gate before prod apply, an over-privileged shared apply role, and an editable pipeline anyone can change.",
    systems: ["GitHub Actions / GitLab CI / Jenkins, or Atlantis / HCP Terraform / Spacelift / Env0 (the run engine)", "Cloud OIDC + scoped deploy role", "Environment protection rules / required reviewers"],
    owners: ["Platform / DevOps — owns the IaC pipeline", "Release management — owns the approval gate", "Security engineering — owns the runner-identity scope and SoD"],
    tools: [
      "confirm the workflow uses `terraform plan -out=plan.out` then `terraform apply plan.out` (same artifact), not a bare `terraform apply` that re-plans",
      "GitHub: required reviewers on the `production` environment (gh api repos/{org}/{repo}/environments/production) — approver ≠ author",
      "inspect the apply-stage cloud role: scoped to the stack's resources + short-lived (OIDC), not AdministratorAccess",
      "confirm `terraform apply` is blocked outside CI (state-lock + no human apply credentials) and the pipeline YAML is branch-protected",
    ],
    finding: "The apply stage re-plans instead of applying the reviewed `plan.out` (so a dependency or data-source change can apply something different from what was approved), there's no required approver before the prod apply, and the runner assumes an AdministratorAccess role usable against the entire account.",
    refs: [["NIST SP 800-204D — CI/CD security", "https://csrc.nist.gov/pubs/sp/800/204/d/final"], ["HashiCorp — Remote operations & run workflow", "https://developer.hashicorp.com/terraform/cloud-docs/run/remote-operations"], ["OWASP Top 10 CI/CD Security Risks", "https://owasp.org/www-project-top-10-ci-cd-security-risks/"]],
  },
  "iac-09": {
    artifacts: [
      "The GitOps controller config (Argo CD / Flux) — the source repos it syncs, the target clusters, and the sync/self-heal/prune settings",
      "The repository trust + access model for the GitOps manifest repos (branch protection, signed commits, who can write desired state)",
      "The controller's in-cluster RBAC + the credentials it uses to reach target clusters (scope of what the agent can change)",
      "Drift/self-heal evidence: out-of-band cluster changes that Argo/Flux detected and reverted, and the sync audit history",
    ],
    test: "Verify the GitOps pull-based delivery model is secured end to end (git is the source of truth, the controller is the only thing that writes to the cluster). PASS: the GitOps repo is the single write path — direct `kubectl apply` to clusters is restricted; the manifest repo has branch protection + required review (and ideally signed/verified commits); the controller verifies commit signatures and only syncs trusted, pinned sources; self-heal/auto-sync reverts drift; the controller's cluster RBAC is least-privilege per target (no single agent with cluster-admin on every cluster); and the controller's own UI/API is authenticated + RBAC-scoped. Exceptions: unprotected manifest repo (any push reaches prod clusters), no commit verification, the controller holding cluster-admin across all clusters, self-heal disabled (so manual drift persists), and a publicly-exposed or weakly-authenticated Argo/Flux endpoint.",
    systems: ["Argo CD / Flux CD (the GitOps controller)", "Git manifest/config repos (GitHub/GitLab) + branch protection + signed commits", "Kubernetes RBAC (the controller's per-cluster permissions)", "Sigstore / Kyverno (image + commit verification)"],
    owners: ["Platform / SRE — owns the GitOps controller and clusters", "AppSec — owns the manifest-repo trust + signing", "Security engineering — reviews the controller's RBAC blast radius"],
    tools: [
      "Argo CD: argocd app list + argocd app get → source repo/revision, auto-sync + self-heal + prune flags per app",
      "Flux: flux get sources git / flux get kustomizations → source pinning + sync status; confirm `verify` (commit signature) is set",
      "kubectl get clusterrolebinding -o wide  → the Argo/Flux service account's RBAC (is it cluster-admin everywhere?)",
      "confirm the manifest repo has branch protection + required review, and direct kubectl write to prod clusters is blocked (the controller is the only writer)",
    ],
    finding: "Argo CD syncs from a manifest repo with no branch protection (any developer can push a manifest straight to the production cluster), commit verification is off, self-heal is disabled so manual drift persists, and the single Argo service account holds cluster-admin on every connected cluster — one repo write or one controller compromise owns the whole fleet.",
    refs: [["Argo CD — Security", "https://argo-cd.readthedocs.io/en/stable/operator-manual/security/"], ["CNCF / OpenGitOps Principles", "https://opengitops.dev/"], ["NSA/CISA Kubernetes Hardening Guide", "https://media.defense.gov/2022/Aug/29/2003066362/-1/-1/0/CTR_KUBERNETES_HARDENING_GUIDANCE_1.2_20220829.PDF"]],
  },
};
